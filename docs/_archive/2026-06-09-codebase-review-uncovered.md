# Code review — domini scoperti (run 2026-06-09)
Review dei domini lasciati NON verificati dalla review precedente (bloccata da rate-limit). Verifica resiliente: i finding non verificabili sono marcati **UNVERIFIED** (da ri-verificare a mano), non scartati.

**Sintesi:** 77 confermati/plausibili (55 CONFIRMED, 22 PLAUSIBLE) + 18 da ri-verificare.

Distribuzione confermati — severità: {'high': 28, 'medium': 27, 'low': 22}; categoria: {'correttezza': 49, 'architettura': 6, 'performance': 14, 'sicurezza': 8}.

> NB: la fase di verifica di `routes/` e `utils/` e l'agente di sintesi sono stati interrotti dal limite di sessione; per quei target i finding sono nella sezione **Da ri-verificare**.

---

## Finding confermati / plausibili

### Severità: HIGH (28)

- **[sicurezza] (PLAUSIBLE)** `models/challenge/services.py:303`
  - **Problema:** create_x_replacement_attempt (e start_challenge_attempt) non verificano che user_id abbia effettivamente un bye nel turno gara_id/round_number indicato. La route create_x_replacement ha solo @login_required. Un giocatore puo' fabbricare una GaraByeChallenge e, completandola, generare un bye Match con se stesso come winner_id in una gara/turno qualsiasi.
  - **Scenario:** Player A (iscritto o anche solo loggato) invia POST /challenge/x-replacement/<gara_id>/<round_number> con un gara_id/round_number arbitrari in cui NON ha un bye. Viene creato un ChallengeAttempt + GaraByeChallenge; poi POST .../complete crea un Match is_bye=True con player1_id=A, winner_id=A e punteggio positivo, alterando la classifica di una gara a cui A non avrebbe titolo per quel bye. IDOR/authz mancante a livello service.
  - **Verifica:** Il difetto a livello di codice e' reale e costruibile. In models/challenge/services.py, create_x_replacement_attempt (riga 304) -> start_challenge_attempt (riga 184) -> GaraByeChallenge.create_for_bye (models/competition/gara_bye_challenge.py:102) non verificano MAI che user_id abbia effettivamente un bye nel gara_id/round_number passati. La route routes/challenge.py:325 create_x_replacement ha solo @login_required: non controlla esistenza gara, iscrizione, ruolo o ownership del bye. In completamento (complete_x_replacement, routes/challenge.py:378) il controllo attempt.user_id == current_user…
- **[sicurezza] (PLAUSIBLE)** `models/shared/email_service.py:88`
  - **Problema:** Stored/reflected XSS via HTML email: user.username viene interpolato raw in una f-string HTML (send_verification_email riga 88, send_password_reset_email riga 106) senza alcun escaping. Lo username non ha restrizioni di carattere (models/user/models.py: solo String(80), unicita'), e la route /auth/register (routes/auth.py:92) accetta request.form['username'] senza validazione di pattern.
  - **Scenario:** Un utente si registra con username come <img src=x onerror=...> o <a href=javascript:...>. Quando l'email di verifica (o reset, se l'attaccante conosce/possiede l'indirizzo) viene renderizzata in un client che esegue HTML, il markup iniettato viene interpretato. Inoltre uno username con tag puo' rompere/spoofare il layout dell'email (phishing del link di verifica). Andrebbe usato markupsafe.escape() su username (e su tutti i campi utente) prima dell'interpolazione, o un template Jinja con autoescape.
  - **Verifica:** Confirmed at code level. In models/shared/email_service.py, user.username is interpolated raw into HTML f-strings at line 88 (send_verification_email) and line 106 (send_password_reset_email), and user.email appears unescaped in the same bodies. The resulting html_content is passed directly to flask_mail.Message(html=...) (line 63) and sent verbatim — no markupsafe.escape() and no Jinja autoescaping anywhere in the chain (grep for escape/markupsafe/render_template returned nothing). Reachability is confirmed: username is db.Column(db.String(80)) with no @validates (models/user/models.py:40); t…
- **[sicurezza] (CONFIRMED)** `routes/player/matches.py:108`
  - **Problema:** IDOR/authz: forfeit_trio accetta forfeiting_player_id dalla form e lo passa a TrioMatchService.forfeit_trio senza verificare che corrisponda a current_user. Il decoratore trio_player_required controlla solo che il richiedente sia uno dei 3 giocatori del trio; il service (handle_forfeit in models/match/models.py:734) valida solo che forfeiting_player_id sia nel trio, mai che added_by_id == forfeiting_player_id.
  - **Scenario:** Il giocatore A (membro del trio) invia POST /match/<id>/trio/forfeit con player_id=<id di B>. Il sistema registra il forfait di B contro la sua volonta: B perde i rack rimanenti assegnati agli avversari. Un partecipante puo sabotare/eliminare un altro giocatore della stessa partita trio.
  - **Verifica:** Real IDOR / broken-authorization bug, constructible end-to-end from the code.

Chain:
- routes/player/matches.py:108 reads forfeiting_player_id = request.form.get("player_id", type=int) straight from the POST body, defaulting to current_user.id only when absent (line 111), then calls TrioMatchService.forfeit_trio(trio_id, forfeiting_player_id, current_user.id) (line 114).
- The decorator trio_player_required (utils/permissions.py:253) only checks current_user.id in [trio.player1_id, trio.player2_id, trio.player3_id] — i.e. ANY of the three trio members passes.
- TrioMatchService.forfeit_trio (…
- **[sicurezza] (CONFIRMED)** `routes/player/challenges.py:81`
  - **Problema:** Authz mancante per gare standalone in record_challenge_attempt: il controllo iscrizione viene eseguito solo se gara_challenge.gara.campionato e' truthy. Per gare standalone (campionato_id=None) qualunque utente autenticato puo registrare tentativi. Anche GaraChallengeService.record_challenge_attempt (gara_challenge_service.py:199) non verifica l'iscrizione/autorizzazione dell'utente.
  - **Scenario:** Un utente non iscritto a una gara standalone con challenge invia POST /challenge/<gara_challenge_id>/attempt con uno score arbitrario. Il tentativo viene registrato e GaraChallengeService.update_gara_classification aggiorna la classifica challenge, inquinando i risultati con un punteggio di un estraneo.
  - **Verifica:** Bug reale e costruibile dal codice.

routes/player/challenges.py:81 — in `record_challenge_attempt` il controllo di iscrizione e' annidato dentro `if gara_challenge.gara.campionato:` (righe 81-94). Per una gara standalone (`campionato_id=None`, quindi `gara.campionato` falsy) l'intero blocco e' saltato: nessuna verifica che `current_user` sia iscritto alla gara. Qualunque utente autenticato (solo `@login_required`, nessun `@player_only` ne before_request che verifichi l'iscrizione per-entita') puo inviare POST /player/challenge/<id>/attempt con score arbitrario.

Il livello service non chiude …
- **[correttezza] (CONFIRMED)** `models/matchmaking/strategies/direct_elimination.py:151`
  - **Problema:** Le query del round successivo filtrano solo status="completed", ignorando MatchStatus.VALIDATED. Ma can_start_new_round() (models.py:339) e update_round_progression (round_service.py:201) considerano VALIDATED come stato finale, quindi il direttore puo' avanzare di turno con match validati. In quel caso previous_matches non include i validati mentre total_previous_matches (riga 156, senza filtro status) li conta: len(previous_matches) != total -> ritorna [] e il round 2 non viene mai generato.
  - **Scenario:** Gara a eliminazione diretta. Il direttore (o la conferma bilaterale dei giocatori) porta i match del round 1 da 'completed' a 'validated'. Si chiede la creazione del round 2: la strategia conta 4 match totali ma trova 0 'completed', ritorna lista vuota -> nessuna coppia generata, torneo bloccato.
  - **Verifica:** Bug reale e costruibile dal codice. In models/matchmaking/strategies/direct_elimination.py:151-153 la query dei vincitori del turno precedente filtra solo status="completed", mentre total_previous_matches (line 156-158) conta tutti i match senza filtro status. Se anche un solo match del turno precedente è in stato "validated" (e non "completed"), len(previous_matches) != total_previous_matches alla riga 160 → ritorna [] e il turno successivo non viene generato.

Lo stato "validated" è raggiungibile per i match di gara (non solo individual): la conferma bilaterale dei giocatori via route /playe…
- **[correttezza] (CONFIRMED)** `models/matchmaking/strategies/double_knockout.py:372`
  - **Problema:** La distinzione winners/losers bracket si basa su Match.notes ('losers_bracket'), ma il campo notes del Pairing non viene mai impostato dalla strategia ne' persistito su Match.notes da create_matches_from_pairings (round_creation.py non copia pairing.notes). Quindi: (a) il filtro notes=='losers_bracket' (riga 372) restituisce sempre vuoto -> i survivor del losers bracket non vengono mai recuperati; (b) il filtro notes!='losers_bracket' (righe 245, 347) in SQL esclude le righe con notes IS NULL, cioe' TUTTI i match winners-bracket reali -> winners non trovati. Inoltre le query usano anche status="completed" (stesso problema VALIDATED del finding precedente).
  - **Scenario:** Gara a doppia eliminazione oltre il round 1. Alla generazione del losers bracket nessun perdente recente viene raccolto (NULL escluso dal !=), e i survivor del losers bracket precedente non vengono mai trovati (== 'losers_bracket' senza match taggati): le coppie risultano errate o vuote, il bracket dei ripescaggi non funziona.
  - **Verifica:** Bug reale e reachable. Il Match model NON ha alcun campo `notes` (verificato: models/match/models.py, base_match.py — `notes` esiste solo su Set in set_models.py:358). Inoltre `pairing.notes` (definito su Pairing in strategies/base.py:28) non viene mai impostato dalla strategia ne' copiato su Match in round_creation.py (nessun riferimento a pairing.notes nella creazione partite). Quindi la distinzione winners/losers bracket basata su Match.not=='losers_bracket' (riga 372) e Match.notes!='losers_bracket' (righe 245, 347) e' completamente non funzionante.

Runtime confermato: `Match.notes` solle…
- **[correttezza] (CONFIRMED)** `models/matchmaking/strategies/round_robin.py:137`
  - **Problema:** Lo schedule round-robin per numero DISPARI di giocatori non ruota gli accoppiamenti: ad ogni turno cambia solo il giocatore in bye (player_ids[round_num]), ma active_players resta l'ordine fisso filtrato e viene accoppiato sempre simmetricamente (i vs -(i+1)). Cosi' molte coppie si ripetono ogni turno e altre coppie non si incontrano mai, violando l'invariante 'tutti contro tutti'. Il ramo pari invece ruota correttamente.
  - **Scenario:** Round robin con 5 giocatori [p0..p4]. Turno1 bye=p0, attivi [p1,p2,p3,p4] -> coppie (p1,p4),(p2,p3). Turno2 bye=p1, attivi [p0,p2,p3,p4] -> coppie (p0,p4),(p2,p3): p2 vs p3 si ripete. Alla fine alcuni giocatori non si affrontano mai e altri piu' volte: classifica falsata.
  - **Verifica:** Real, constructible bug at models/matchmaking/strategies/round_robin.py lines 137-143 (odd-player branch). For odd N, active_players is recomputed each round from the FIXED original player_ids order (line 137: [p for p in player_ids if p != bye_player]) and then paired symmetrically (lines 140-142: active[i] vs active[-(i+1)]). Only the bye rotates (line 133: player_ids[round_num]); the pairing geometry never rotates, so the same pairs recur every round.

I reproduced the finding's exact scenario with 5 players: Round1 bye=p0 -> (p1,p4),(p2,p3); Round2 bye=p1 -> (p0,p4),(p2,p3); etc. Result ov…
- **[correttezza] (CONFIRMED)** `models/campionato/tournament_service.py:567`
  - **Problema:** terminate_campionato tratta ROUND_COMPLETED come stato terminale: per una gara che ha solo completato un round (torneo ancora in corso) tenta StateService.complete(); se la transizione fallisce, l'except: pass cade su gara.soft_delete('Campionato terminato'), eliminando una gara con match realmente giocati. Anche in caso di successo, promuove a COMPLETED una gara mid-tournament violando l'invariante di dominio.
  - **Scenario:** Director termina un campionato mentre una gara ha completato il round 1 di 3 (derived = ROUND_COMPLETED). StateService.complete solleva (transizione non lecita perche' restano round); il fallback soft-elimina la gara. I dati di quella gara (RoundClassification/match completati) spariscono dalla classifica del campionato e dai playoff, oppure la gara viene chiusa a meta'.
  - **Verifica:** Bug reale e costruibile dal codice. In `terminate_campionato` (models/campionato/tournament_service.py:563-575) `terminal_states` include `ProvaDerivedStatus.ROUND_COMPLETED.value` (riga 565), ma ROUND_COMPLETED indica per definizione una gara a meta' torneo (round finito, altri round da giocare).

Entrambi i rami denunciati dal finding sono confermati:

1) Promozione errata a COMPLETED. `StateService.complete` (models/competition/state_service.py:127-148) verifica SOLO status PLAYING/AWAITING_SSR e l'assenza di match PENDING/PLAYING — NON controlla `current_round >= rounds_count`. Una gara co…
- **[correttezza] (CONFIRMED)** `models/kpi/metrics_service.py:169`
  - **Problema:** get_active_users_count restituisce active_p1 + active_p2: gli utenti che nel periodo compaiono sia come player1 sia come player2 vengono contati due volte. Questa funzione alimenta DAU/WAU/MAU e quindi anche la stickiness (dau/mau) in CommunityService.get_community_health.
  - **Scenario:** Un utente gioca nel periodo un match come player1 e un altro come player2: viene contato 2 volte. Con community piccola e molti cross-match, MAU puo' superare il numero reale di utenti e la stickiness dau/mau puo' superare il 100% (es. dau=3, mau=2 -> 150%), producendo metriche dashboard palesemente errate.
  - **Verifica:** Real bug. In models/kpi/metrics_service.py:169 get_active_users_count returns `active_p1 + active_p2`, where active_p1 = COUNT(DISTINCT player1_id) and active_p2 = COUNT(DISTINCT player2_id) computed as two independent queries (lines 143-167). The distinct counts are per-column, not across the union of player1/player2, so any user who in the period appears at least once as player1 AND at least once as player2 is counted in both terms (double-counted). Correct implementation would union the two id sets (e.g. COUNT(DISTINCT) over a UNION of player1_id/player2_id, like the existing get_inactive l…
- **[correttezza] (CONFIRMED)** `models/playoff/services.py:189`
  - **Problema:** find_replacement_player filtra status con .value ('confirmed'/'pending'), ma la colonna db.Enum(QualificationStatus) persiste il NOME del membro ('CONFIRMED'/'PENDING') — verificato sul DB. Le due query (righe 189 e 192) non matchano mai alcuna riga, quindi current_players e' sempre vuoto.
  - **Scenario:** Un giocatore rifiuta/scade e si invoca find_replacement_player. Poiche' current_players e' vuoto, il loop su evaluate_qualifications() restituisce il PRIMO qualificato in assoluto — quasi sempre un giocatore gia' CONFIRMED/PENDING — creando una PlayoffQualification DUPLICATA per chi e' gia' nel playoff invece di pescare il vero sostituto in coda. Tutto il resto del file filtra correttamente con l'oggetto enum (es. righe 308, 350, 494): qui c'e' la sola incongruenza.
  - **Verifica:** Bug reale e dimostrato. In models/playoff/models.py:359 la colonna è `status = db.Column(db.Enum(QualificationStatus), ...)` senza `values_callable` (verificata assenza in tutto il codebase). Con un Python Enum nativo, SQLAlchemy di default persiste il NOME del membro, non il `.value`. Conferma sul DB reale: `SELECT DISTINCT status FROM playoff_qualification` restituisce DECLINED/PENDING/CONFIRMED (nomi maiuscoli), non i valori lowercase. Test runtime sui dati reali: `filter_by(status=QualificationStatus.CONFIRMED.value)` (stile riga 189) → 0 righe; `filter_by(status=QualificationStatus.CONFIR…
- **[correttezza] (CONFIRMED)** `models/playoff/models.py:404`
  - **Problema:** decline_participation() (riga 404) ed expire_qualification() (riga 418) chiamano self.configuration._find_replacement() protetto da hasattr(...): ma PlayoffConfiguration NON definisce _find_replacement (esiste solo PlayoffService.find_replacement_player). La guardia hasattr e' sempre False, quindi entrambi i metodi ritornano sempre None.
  - **Scenario:** Un giocatore rifiuta l'invito via /player/playoff/decline -> decline_qualification -> decline_participation() ritorna None -> in services.py riga 170 'if replacement:' e' sempre falso -> nessun sostituto viene mai cercato/invitato quando un qualificato declina. Il flusso di rimpiazzo documentato (decline -> invite next eligible) e' codice morto sul path del rifiuto giocatore.
  - **Verifica:** Bug reale e costruibile dal codice.

models/playoff/models.py:404 (decline_participation) e :418 (expire_qualification) chiamano self.configuration._find_replacement() solo se hasattr(self.configuration, "_find_replacement"). La classe PlayoffConfiguration (definita a models.py:42, termina dove inizia PlayoffQualification a :339) NON definisce _find_replacement: l'unico metodo di rimpiazzo e' PlayoffService.find_replacement_player (services.py:177). Quindi hasattr e' sempre False ed entrambi i metodi ritornano sempre None.

Path del rifiuto giocatore confermato reachable e privo di fallback:
-…
- **[correttezza] (CONFIRMED)** `models/rating/handicap_service.py:83`
  - **Problema:** create_handicap_rule costruisce RatingHandicapRule con kwargs inesistenti (`rating_difference_threshold`, `handicap_per_point`). Il modello RatingHandicapRule (models.py:261-291) NON ha questi campi: ha `min_difference`, `max_difference`, `points_per_handicap`, `max_handicap`. Inoltre legge anche `rating_rule_data['rating_difference_threshold']` e `['handicap_per_point']` dal dict in input, chiavi diverse da quelle prodotte altrove.
  - **Scenario:** Un admin chiama create_handicap_rule passando rating_rules (es. dalla UI di gestione handicap). SQLAlchemy/db.Model riceve kwargs sconosciuti -> TypeError 'invalid keyword argument' (o colonne min_difference/points_per_handicap NOT NULL non valorizzate -> IntegrityError). La creazione della regola fallisce sempre. Le rating_rules non sono mai create correttamente. Nota: create_standard_handicap_rule (riga 266) usa invece i nomi giusti, confermando il copia-incolla sbagliato.
  - **Verifica:** Bug reale e costruibile. In models/rating/handicap_service.py:83-91, create_handicap_rule costruisce RatingHandicapRule(rating_difference_threshold=..., handicap_per_point=...). Il modello RatingHandicapRule (models/rating/models.py:261-291) NON ha questi campi: ha min_difference, max_difference, points_per_handicap, max_handicap. RatingHandicapRule estende BaseModel(db.Model) (base.py:182), quindi il costruttore declarative di SQLAlchemy solleva TypeError: 'rating_difference_threshold' is an invalid keyword argument al primo kwarg sconosciuto (riga 86). Inoltre legge rating_rule_data['rating_…
- **[correttezza] (CONFIRMED)** `models/rating/calculation_service.py:39`
  - **Problema:** process_match_result non e' idempotente: ogni invocazione applica un nuovo delta Elo e incrementa games_played (update_rating, models.py:122). L'handler si attiva ad ogni to_completed senza guardia contro la ri-elaborazione dello stesso match.
  - **Scenario:** Admin resetta un match completato a pending e lo ricompleta (flusso supportato: reset_to_pending -> add_rack -> to_completed), oppure lo script scripts/recalc_elo.py viene eseguito su match gia' processati. MatchCompletedEvent viene riemesso, process_match_result ricalcola il delta sui rating gia' aggiornati e ri-incrementa games_played: il rating dei giocatori si gonfia/sgonfia in modo cumulativo e games_played diventa errato. Non esiste un flag tipo rating_processed ne' un MatchRatingHistory per de-duplicare.
  - **Verifica:** Bug reale e costruibile dal codice. La catena e' completa e priva di qualsiasi guardia di idempotenza:

1. models/rating/event_handlers.py:41 — handle_match_completed chiama RatingCalculationService.process_match_result(match) ad ogni MatchCompletedEvent, senza controllare se quel match e' gia' stato elaborato per il rating (nessun flag rating_processed, nessuna tabella di history: grep su "rating_processed|RatingHistory|already.*processed" non trova nulla).

2. models/match/state_service.py:114-125 — to_completed setta sempre lo status a COMPLETED e chiama _emit_completion_event, che a riga 1…
- **[correttezza] (CONFIRMED)** `models/location/services.py:62`
  - **Problema:** find_or_create_venue muta il venue (is_active=False, verified=False, set_table_names) DOPO che create_billiard_hall (@transactional) ha gia' committato la riga con i default (is_active=True). Queste mutazioni restano pendenti nella sessione senza una transaction che le chiuda: vengono persistite solo per side-effect del successivo GaraService.create_gara nella route. Se tra la creazione del venue (riga 75 della route) e create_gara avviene un early-return (validate_strategy fallisce, riga 78), le mutazioni vengono scartate al teardown e il venue resta committato come ATTIVO.
  - **Scenario:** Un director crea una gara standalone indicando un luogo nuovo + tavoli, ma la configurazione strategia e' invalida. find_or_create_venue committa il BilliardHall con is_active=True (default), poi imposta is_active=False/verified=False/table_names solo in memoria; validate_strategy ritorna errori e la route fa redirect senza chiamare create_gara. Le mutazioni pendenti non vengono mai committate: in DB resta un venue ATTIVO e non verificato che compare subito nelle liste pubbliche (get_nearby_halls/search filtrano is_active=True) e nei suggerimenti, bypassando il flusso di verifica admin.
  - **Verifica:** Bug reale e costruibile dal codice. Catena verificata:

1) models/location/services.py:63 — `create_billiard_hall` è `@transactional(domain="location")` (riga 82): al ritorno fa commit, persistendo il BilliardHall con `is_active=True` (default del modello) e `verified=False`.
2) services.py:68-71 — DOPO il commit, `find_or_create_venue` (che NON è `@transactional`) muta `new_venue.set_table_names(...)`, `is_active=False`, `verified=False`. Con `expire_on_commit=False` (config.py:85) l'istanza resta in memoria e queste riassegnazioni la rendono dirty, ma restano PENDENTI: nessuna transaction le…
- **[correttezza] (CONFIRMED)** `models/challenge/services.py:414`
  - **Problema:** _create_x_replacement_match_result imposta match.player1_score = max(1, attempt.score or 0), cioe' lo score grezzo della challenge usato come numero di rack vinti. I bye match normali usano invece il round_distance (winning_score) come player1_score (vedi round_creation.py:142/150). Una challenge numerica ha scala arbitraria (es. 0-15) scollegata da gara.distance.
  - **Scenario:** Gara 'race to 5' (distance=5). Un giocatore in bye fa la X-replacement su una challenge 'spot shot' con punteggio 12/15. complete_x_replacement_attempt crea/aggiorna il bye match con player1_score=12 (12 rack vinti in un race-to-5, dove il massimo e' 5). La RoundClassification calcola differenza rack/punti su un valore impossibile, inflazionando la posizione del giocatore in classifica rispetto agli altri bye (che hanno player1_score=5). Inoltre viola ADR-027: lo score non passa per match.distance_config/effective_distance.
  - **Verifica:** Bug reale e costruibile. In models/challenge/services.py:414 `_create_x_replacement_match_result` imposta `match.player1_score = max(1, attempt.score or 0)` su un bye match (is_bye=True, status completed). Questo score grezzo della challenge ha scala arbitraria (Challenge.max_score, es. 0-15) scollegata da gara.distance. Il bye normale invece usa `player1_score = winning_score = round_distance` (models/competition/round_creation.py:128). Lo score viene poi consumato da ScoreAggregator._process_bye_match (models/classification/score_aggregator.py:306: `player_stats[pid]["racks_won"] += match.pl…
- **[correttezza] (CONFIRMED)** `models/exam/models.py:144`
  - **Problema:** Exam.get_max_possible_score() legge exam_challenge.challenge.max_score, ma il modello Challenge NON ha una colonna max_score (ha solo description, image_path, pass_fail_only, is_active, created_by_id). L'accesso solleva AttributeError. Stesso problema in ExamChallenge.get_weighted_max_score() (line 180) e di conseguenza in ExamAttempt.complete_exam() (line 252) che lo invoca.
  - **Scenario:** Un director crea un exam con almeno una challenge e uno studente lo completa: complete_exam() -> get_weighted_max_score() -> self.challenge.max_score -> AttributeError. Il calcolo del voto finale e del max_possible_score crasha. La doc challenge/CLAUDE.md elenca max_score come campo ma il modello reale non lo definisce: tutto il flusso di grading e' rotto.
  - **Verifica:** Bug reale. Il modello Challenge (models/challenge/models.py:73-102) definisce solo le colonne id, description, image_path, pass_fail_only, created_by_id, is_active (+ created_at/updated_at). NON ha alcuna colonna o attributo max_score: confermato a runtime (hasattr(Challenge,'max_score')==False) e sul DB live (PRAGMA table_info(challenge) non contiene max_score). Nessuna migrazione lo aggiunge. Il valore max_score esiste solo come variabile locale (max_score_achieved) dentro Challenge.get_statistics(), non come attributo del modello, e la doc challenge/CLAUDE.md lo elenca erroneamente tra i ca…
- **[correttezza] (CONFIRMED)** `models/exam/models.py:184`
  - **Problema:** ExamChallenge.__repr__ usa self.challenge.name e ExamChallengeResult.__repr__ (line 321) usa self.exam_challenge.challenge.name, ma Challenge non ha una colonna name. Qualunque repr/log/debug di queste entita' solleva AttributeError.
  - **Scenario:** Logging, debug, o un f-string che includa l'oggetto (es. in un messaggio d'errore o in una shell) invoca __repr__ e crasha con AttributeError invece di mostrare l'oggetto. Anche tracebacks che renderizzano l'oggetto possono mascherare l'errore originale.
  - **Verifica:** Bug reale e riproducibile. Il modello `Challenge` (models/challenge/models.py, righe 73-102) NON ha alcuna colonna o property `name`: le colonne sono id, description, image_path, pass_fail_only, created_by_id, is_active (+ max_score, created_at, updated_at). Verificato empiricamente: `'name' in dir(Challenge)` -> False e `Challenge().name` -> `AttributeError: 'Challenge' object has no attribute 'name'`. Di conseguenza:
- `ExamChallenge.__repr__` (models/exam/models.py:184) usa `self.challenge.name` e crasha con AttributeError.
- `ExamChallengeResult.__repr__` (models/exam/models.py:321) usa `s…
- **[correttezza] (CONFIRMED)** `models/caching/manager.py:223`
  - **Problema:** _evict_entries non gestisce la strategia HYBRID: la catena if/elif copre solo LRU/LFU/FIFO/TTL. Per HYBRID nessun elemento viene rimosso, ma _stats.evictions += 1 viene comunque eseguito. Il livello L2_APPLICATION e' registrato proprio con strategy=CacheStrategy.HYBRID (riga 440), quindi il suo max_size=2000 non viene mai applicato.
  - **Scenario:** Con uso prolungato dell'app, le entry di classificazione/campionato finiscono in L2 (HYBRID). Quando len(_cache) raggiunge 2000, set() chiama _evict_entries() che non rimuove nulla; la cache L2 cresce illimitatamente oltre max_size finche' il processo non esaurisce memoria. Inoltre evictions viene incrementato falsamente, falsando le statistiche.
  - **Verifica:** Bug reale e costruibile dal codice in models/caching/manager.py.

_evict_entries() (righe 223-252) ha una catena if/elif che copre solo LRU (225), LFU (228), FIFO (234) e TTL (237). Non esiste alcun ramo per CacheStrategy.HYBRID (enum definito a riga 35). Quando strategy == HYBRID nessun elemento viene rimosso da _cache, ma la riga 252 `self._stats.evictions += 1` viene eseguita SEMPRE, fuori dall'if/elif, incrementando falsamente il contatore evictions.

Il livello L2_APPLICATION è registrato proprio con strategy=CacheStrategy.HYBRID e max_size=2000 (riga 440), quindi il difetto è raggiungibi…
- **[correttezza] (CONFIRMED)** `models/classification/gara_classification.py:26`
  - **Problema:** get_round_standings(gara_id, round_number) usa key_generator='gara', che genera la chiave f'gara:{gara_id}' (gara_cache_key in manager.py:528) ignorando round_number. Tutti i round della stessa gara condividono la stessa chiave di cache.
  - **Scenario:** Si chiama get_round_standings(gara_id=5, round_number=1): il risultato viene messo in cache sotto 'gara:5'. Si chiama poi get_round_standings(gara_id=5, round_number=2): cache_manager.get('gara:5') ritorna le standing del round 1, che vengono restituite come se fossero del round 2. Le classifiche di round mostrate (e usate per il matchmaking Amalfi) sono sbagliate finche' il TTL di 900s non scade.
  - **Verifica:** Bug reale e costruibile dal codice. In models/classification/gara_classification.py:26, get_round_standings(gara_id, round_number) usa @cached(key_generator="gara"). Il generator gara_cache_key (models/caching/manager.py:528-531) ritorna f"gara:{gara_id}" usando SOLO gara_id e ignorando round_number. Quindi tutti i round della stessa gara collidono sulla stessa chiave di cache.

La cache e' reale e attiva, non un no-op: HierarchicalCacheManager (manager.py:430-441) registra due backend in-memory persistenti a livello di processo (L1 LRU + L2 HYBRID), e get/set rispettano il TTL di 900s (is_exp…
- **[correttezza] (CONFIRMED)** `models/caching/manager.py:463`
  - **Problema:** Il decorator cached usa 'if cached_value is not None' per decidere hit/miss e cache_manager.get() ritorna None sia per miss sia per valore memorizzato None. I valori None non vengono mai serviti dalla cache e, su path che ritornano None (es. get_player_ranking -> Optional[Classification]), la funzione viene rieseguita ogni volta. Una entry None comunque salvata e' indistinguibile da un miss.
  - **Scenario:** get_player_ranking(campionato_id, user_id) per un giocatore non classificato ritorna None. set() salva una entry con value=None, ma ogni get successivo (cache_manager.get) ritorna entry.value == None, che il decorator interpreta come miss e riesegue la query DB. Il caching e' di fatto disabilitato per tutti i risultati None/falsy-None, vanificando l'ottimizzazione.
  - **Verifica:** Bug reale e costruibile dal codice. In models/caching/manager.py la `get()` di livello manager (riga 315) ritorna `entry.value` su hit e `None` su miss (riga 318): un valore memorizzato `None` e un miss sono indistinguibili. Il decorator `cached` alla riga 463 fa `if cached_value is not None: return cached_value`, quindi qualunque risultato `None` salvato cade comunque nel ramo che riesegue la funzione (riga 467) e fa di nuovo `set()`. Funzioni cache-ate che ritornano `None` esistono e sono raggiungibili: es. `CampionatoClassificationService.get_player_ranking` (campionato_classification.py:17…
- **[correttezza] (CONFIRMED)** `models/events/notification_handlers.py:213`
  - **Problema:** handle_venue_manager_request_processed tratta come 'rifiutata' ogni status diverso da 'approved'. Ma VenueManagerService.revoke_venue_assignment (venue_manager_service.py:452) pubblica VenueManagerRequestProcessedEvent con status='revoked'. Il ramo else genera titolo 'Richiesta Gestore ... Rifiutata' e messaggio 'la tua richiesta ... è stata rifiutata'.
  - **Scenario:** Un admin revoca la gestione sala a un gestore esistente. Il gestore (che NON aveva alcuna richiesta pendente) riceve una notifica fuorviante 'Richiesta Gestore X Rifiutata' / 'la tua richiesta di gestire ... è stata rifiutata. Per maggiori informazioni contatta l'amministratore', invece di un messaggio di revoca. Comunicazione errata all'utente.
  - **Verifica:** Bug reale e costruibile. VenueManagerService.revoke_venue_assignment (models/user/venue_manager_service.py:445-456) pubblica VenueManagerRequestProcessedEvent con status="revoked". L'handler handle_venue_manager_request_processed e' registrato a quell'evento (models/events/notification_handlers.py:66-67) e ha solo due rami: `if event.status == "approved"` (riga 213) e un `else` (riga 219) commentato/templato come "rejected". Poiche' "revoked" != "approved", la revoca cade nell'else e produce title `Richiesta Gestore '...' Rifiutata` (riga 220) e message `La tua richiesta di gestire la sede '..…
- **[correttezza] (CONFIRMED)** `models/transaction/manager.py:282`
  - **Problema:** Il fallimento del commit nel ramo non-nested viene inghiottito: l'eccezione di db.session.commit() e' catturata e loggata solo come warning (linee 285-288), poi l'esecuzione prosegue a context.status = COMMITTED (linea 290) e il context manager esce normalmente. La funzione decorata con @transactional ritorna il proprio valore come se tutto fosse andato a buon fine.
  - **Scenario:** Un servizio @transactional crea/modifica entita' e ritorna. Al commit scatta un IntegrityError (UNIQUE/FK violata) o un errore I/O su SQLite (database locked). L'eccezione viene swallowata: la route riceve un ritorno di successo (es. 200/redirect 'salvato'), ma il DB ha fatto rollback automatico e i dati non esistono. Perdita dati silenziosa riportata all'utente come successo.
  - **Verifica:** Bug reale in models/transaction/manager.py. Nel ramo non-nested (else, righe 281-288) un fallimento di db.session.commit() viene catturato e loggato SOLO come warning (logger.warning, righe 286-288), senza re-raise e senza rollback. L'esecuzione prosegue a riga 290 context.status = TransactionStatus.COMMITTED, il context manager esce normalmente (nessuna eccezione raggiunge l'except di riga 292), e il decoratore wrapper (righe 446-453) restituisce al chiamante il valore di ritorno di func() come se fosse andato tutto a buon fine.

Lo scenario e' costruibile da stato realistico: un IntegrityErr…
- **[correttezza] (CONFIRMED)** `models/transaction/manager.py:257`
  - **Problema:** Stesso swallow del commit fallito nei rami nested (linee 238-252) e pseudo-nested (269-280): l'eccezione del commit/release-savepoint e' catturata internamente, si tenta un rollback ma l'errore non viene ri-sollevato. L'outer except (linea 292) che farebbe il re-raise non viene mai raggiunto, quindi il chiamante non sa che lo stato e' stato annullato.
  - **Scenario:** Operazione composta: outer @transactional chiama piu' servizi @transactional (savepoint). Il release del savepoint o il commit del padre fallisce (es. constraint deferred che esplode a fine transazione). Il manager fa rollback ma ritorna senza eccezione: l'aggregato (es. round di matchmaking + classifica) appare creato in memoria/risposta ma e' stato annullato nel DB. Incoerenza tra UI e persistenza.
  - **Verifica:** Bug reale e costruibile dal codice in models/transaction/manager.py. Nei tre rami di commit del context manager `transaction()` l'eccezione del commit/release-savepoint viene catturata localmente e NON ri-sollevata:

- nested (linee 238-252): `db.session.commit()` (riga 234) che fallisce viene loggato, si tenta rollback (riga 243), ma nessun re-raise;
- pseudo-nested (linee 269-280): il commit del savepoint o del parent autobegin (righe 258/264) che fallisce viene loggato, rollback tentato (riga 275), nessun re-raise;
- else top-level (linee 285-288): il commit fallito viene solo loggato, nemm…
- **[correttezza] (CONFIRMED)** `models/player/history_service.py:291`
  - **Problema:** In _calculate_match_stats, per i match regolari somma player1_score/player2_score come 'rack vinti/persi'. Ma per i match multi-set (is_multi_set=True) quei campi rappresentano i SET vinti, non i rack (vedi models/match/CLAUDE.md 'Score Interpretation'). La query base non filtra is_multi_set, quindi i match multi-set finiscono nello storico e inquinano total_racks_won/total_racks_lost.
  - **Scenario:** Un utente ha giocato un match multi-set best-of-5 vinto 3 set a 1. Nello storico partite total_racks_won viene incrementato di 3 e total_racks_lost di 1 (conteggio set), invece dei rack effettivamente giocati nei set. Le statistiche rack mostrate al giocatore sono sbagliate per chiunque abbia disputato match multi-set.
  - **Verifica:** Bug reale e costruibile dal codice. In `models/player/history_service.py::_calculate_match_stats`, il ramo "Regular 2-player match" (righe 291-296) somma incondizionatamente `m.player1_score`/`m.player2_score` in `racks_won`/`racks_lost`:

    if m.player1_id == user_id:
        racks_won += m.player1_score or 0
        racks_lost += m.player2_score or 0
    else:
        racks_won += m.player2_score or 0
        racks_lost += m.player1_score or 0

Non c'e' alcun controllo su `m.is_multi_set`. La documentazione di progetto e' esplicita su questa ambiguita': `models/match/CLAUDE.md` (righe 466-…
- **[correttezza] (CONFIRMED)** `routes/challenge.py:231`
  - **Problema:** In complete_attempt, quando la richiesta e' JSON, `data` e' un dict semplice e `data.get("score", type=int)` / `data.get("passed", type=bool)` sollevano TypeError (dict.get non accetta kwargs). Il blocco except cattura solo ValueError, quindi diventa un 500 non gestito.
  - **Scenario:** Un client invia POST /challenge/attempt/<id>/complete con Content-Type: application/json e body {"score": 5}. request.is_json e' True, data e' un dict, data.get("score", type=int) -> TypeError 'dict.get() takes no keyword arguments'. Non e' ValueError -> 500 Internal Server Error invece di completare il tentativo.
  - **Verifica:** Real, constructible bug in routes/challenge.py complete_attempt. Line 227: `data = request.get_json() if request.is_json else request.form`. When the request is JSON, `request.get_json()` returns a plain Python dict. Lines 231-232 then call `data.get("score", type=int)` and `data.get("passed", type=bool)`. The `type=` kwarg is a Werkzeug MultiDict.get extension valid only for `request.form`; on a plain dict it raises `TypeError: dict.get() takes no keyword arguments` (verified by running it). The route (line 216) is a plain POST guarded only by @login_required, so any logged-in user who owns t…
- **[correttezza] (CONFIRMED)** `routes/challenge.py:233`
  - **Problema:** In complete_attempt (path form), `passed=data.get("passed", type=bool)` usa la coercizione Werkzeug type=bool, che fa bool(str): la stringa "false" (o "0") diventa True. Un tentativo pass/fail FALLITO viene registrato come PASSATO.
  - **Scenario:** Una challenge pass_fail_only riceve un POST form con passed=false (giocatore che ha fallito). md.get('passed', type=bool) restituisce True perche' bool('false') e' True. ChallengeAttempt.complete_attempt(passed=True) marca il tentativo come superato: invariante di dominio violata, statistiche/pass_rate falsate.
  - **Verifica:** Bug reale e costruibile end-to-end.

CATENA:
1. Template templates/player/challenge_attempt_detail.html (linea 156) per challenge pass_fail_only renderizza un radio `<input type="radio" name="passed" value="false">` per il risultato "Fallito". Il form (linea 133) e' un POST classico `method="POST"` senza intercettazione JS, quindi il browser invia letteralmente `passed=false` come stringa form-encoded.

2. Route routes/challenge.py: `data = request.get_json() if request.is_json else request.form` (linea 227). Per il submit del form, `data` e' `request.form` (MultiDict). Alla linea 232: `passed…
- **[performance] (CONFIRMED)** `models/campionato/homepage_service.py:126`
  - **Problema:** La homepage pubblica (hot path anonimo) costruisce, per OGNI campionato mostrato, la classifica generale via calculate_general_classification -> _aggregate_player_totals, che itera tutte le gare del campionato eseguendo query RoundClassification (e GaraClassification per Random) per singola gara. N+1 moltiplicato per campionato + per gara, su una pagina pubblica ad alto traffico.
  - **Scenario:** Homepage con alcuni campionati ciascuno con molte gare completate: una singola GET / esegue decine/centinaia di query (1 per gara per la classifica corrente + 1 per gara per quella precedente in _aggregate_player_totals chiamato due volte), degradando la latenza e il DB sotto carico anonimo.
  - **Verifica:** Bug reale e costruibile dal codice. La homepage pubblica anonima (routes/main.py:13 index(), nessun @login_required) chiama HomepageService.get_homepage_data(). In homepage_service.py:82-84 viene eseguito _build_tournament_data() per OGNI campionato mostrato (tutti gli attivi, senza cap, + fino a 2 completati), e per ciascuno homepage_service.py:125-128 invoca calculate_general_classification(campionato.id). Dentro statistics_service.py, _aggregate_player_totals (riga 126) cicla `for gara in garas` eseguendo una query RoundClassification per singola gara (righe 147-152) + una query GaraClassif…
- **[performance] (CONFIRMED)** `models/dashboard/item_builders.py:57`
  - **Problema:** N+1: build_unified_items esegue una query Gara per OGNI campionato dentro il loop, vanificando il joinedload(gare) gia' fatto da campionatos_q(). La relationship campionato.gare e' gia' caricata in memoria; qui si ri-interroga il DB con db.session.query(Gara).filter(campionato_id==...). Con N campionati = N query extra ad ogni render della dashboard.
  - **Scenario:** Su una dashboard con 30 campionati, ogni caricamento (admin/director/player) emette 30 SELECT aggiuntive inutili sulla tabella gare, nonostante campionatos_q() abbia gia' eager-loaded campionato.gare. Sostituendo con `for g in campionato.gare` si elimina l'N+1. Il docstring del service vanta un 'N+1 fix' ma riguarda solo DirectorAssignment, non questo path.
  - **Verifica:** Bug reale e costruibile dal codice. In models/dashboard/item_builders.py:57-61 il loop `for campionato in campionati` esegue `db.session.query(Gara).filter(Gara.campionato_id == campionato.id).all()` una volta per ogni campionato. I produttori della lista — `campionatos_q()` e `managed_campionatos_q()` in models/dashboard/query_builders.py:33,49 — fanno gia' `joinedload(getattr(Campionato, "gare"))`, e tutti i caller in dashboard_service.py (linee 115, 172, 331, 452) passano `campionatos_q().all()` a `build_unified_items`. Quindi la collection `campionato.gare` e' gia' materializzata in memori…

### Severità: MEDIUM (27)

- **[sicurezza] (CONFIRMED)** `models/location/models.py:181`
  - **Problema:** get_available_users e LocationService.find_available_players costruiscono User.query.join(UserLocationAvailability) senza filtrare gli utenti soft-deleted (deleted_at IS NULL). SoftDeleteMixin NON installa alcun filtro automatico di sessione (verificato: nessun query_class/with_loader_criteria), quindi a differenza di GeoMatchingService.get_players_in_province (che filtra User.deleted_at.is_(None)) questi path restituiscono anche utenti anonimizzati.
  - **Scenario:** Un utente si anonimizza (anonymize() imposta deleted_at) ma aveva availability is_available=True su una sede. find_available_players/get_available_users lo includono ancora tra i giocatori disponibili, esponendo nome/dati di un account cancellato e proponendolo come avversario in match individuali.
  - **Verifica:** Bug reale e costruibile dal codice. models/location/models.py:181 (get_available_users) costruisce User.query.join(UserLocationAvailability).filter(billiard_hall_id==self.id, is_available.is_(True)) SENZA filtrare User.deleted_at.is_(None); idem LocationService.find_available_players (services.py:225-260) che itera le UserLocationAvailability e restituisce availability.user senza alcun filtro soft-delete. La premessa tecnica del finding e' verificata: SoftDeleteMixin (models/base.py:83-117) espone solo deleted_at/soft_delete()/is_deleted, e NON installa alcun filtro automatico di sessione: nel…
- **[sicurezza] (CONFIRMED)** `routes/i18n.py:13`
  - **Problema:** Open redirect: set_language reindirizza a request.referrer validando solo `request.host in next_page` (match di sottostringa). Un URL esterno che contiene l'host come sottostringa supera il controllo.
  - **Scenario:** Un attaccante induce la vittima ad aprire /set_language/it con Referer = http://evil.com/?x=myhost.com. `request.host in next_page` e' True (myhost.com e' sottostringa), quindi redirect(next_page) porta l'utente su evil.com. Va sostituito con un controllo su urlparse(...).netloc rispetto all'host consentito.
  - **Verifica:** routes/i18n.py:13 `if next_page and request.host in next_page` valida il referrer con un match di SOTTOSTRINGA, non per netloc. `request.host` e' l'host nudo (es. `www.torneibiliardo.it`) e `next_page` e' il Referer grezzo, controllabile dall'attaccante. Un URL esterno che contiene l'host come sottostringa passa il controllo, p.es. `https://www.torneibiliardo.it.evil.com/` (l'host e' un prefisso del dominio attaccante) oppure `https://evil.com/?x=www.torneibiliardo.it`. In entrambi i casi `request.host in next_page` e' True e `redirect(next_page)` (riga 14) porta la vittima su evil.com -> open…
- **[sicurezza] (CONFIRMED)** `utils/encryption.py:47`
  - **Problema:** Salt PBKDF2 hardcoded (b"campionati-biliardo-salt") e chiave di default hardcoded "default-development-key-change-in-production" quando ENCRYPTION_KEY non e' impostata. Il salt statico vanifica la difesa contro rainbow table e, soprattutto, se in produzione ci si dimentica di settare ENCRYPTION_KEY i dati PII vengono cifrati con una chiave nota a chiunque legga il repo.
  - **Scenario:** Deploy in produzione senza ENCRYPTION_KEY nell'ambiente: il codice stampa un warning su stdout (facilmente perso nei log) e cifra email/telefono di tutti gli utenti con la chiave pubblica nel sorgente. Chiunque ottenga una copia del DB puo' decrittare tutti i dati personali usando la chiave hardcoded. Manca un fail-fast in produzione quando la chiave non e' configurata.
  - **Verifica:** Bug reale confermato in utils/encryption.py. Riga 40: quando ENCRYPTION_KEY non e' nell'ambiente, il codice usa una chiave hardcoded "default-development-key-change-in-production" (committata nel repo) emettendo solo un print() su stdout (righe 41-43), senza fail-fast in produzione. Riga 47: il salt PBKDF2 b"campionati-biliardo-salt" e' statico e usato incondizionatamente (anche con una vera ENCRYPTION_KEY), vanificando la difesa contro rainbow table / precomputazione. Queste chiavi cifrano PII reali: models/user/models.py:42 (email, EncryptedString) e :51 (phone) passano per EncryptedString.p…
- **[sicurezza] (PLAUSIBLE)** `utils/permissions.py:223`
  - **Problema:** I decoratori di partecipazione (match_player_required, trio_player_required, individual_match_player_required, inscription_owner_required, ecc.) accedono a current_user.id senza verificare current_user.is_authenticated. Dipendono interamente dal fatto che @login_required sia applicato prima. Se in una nuova route si dimentica @login_required, con utente anonimo current_user.id solleva AttributeError (AnonymousUserMixin non ha id) -> 500, e in altri rami il controllo IDOR salterebbe.
  - **Scenario:** Uno sviluppatore aggiunge una route POST con @match_player_required ma dimentica @login_required (errore facile, non c'e' nessun assert). Un client non autenticato chiama l'endpoint: current_user.id va in AttributeError -> 500 invece di redirect a login; in scenari dove il confronto degenera, un utente non autorizzato potrebbe non essere bloccato. I decoratori dovrebbero verificare esplicitamente l'autenticazione come fa venue_manager_required (riga 97).
  - **Verifica:** Verified in utils/permissions.py: match_player_required (line 223), trio_player_required (253), individual_match_player_required (351), inscription_owner_required (278), player_required (195), challenge_attempt_player_required (326) all dereference current_user.id with no current_user.is_authenticated check, relying entirely on @login_required being applied upstream. venue_manager_required (line 97) confirms the codebase knows the safe pattern but the participation decorators don't use it — an inconsistency / defense-in-depth gap, not a current exploit.

Current state is safe: every existing u…
- **[correttezza] (CONFIRMED)** `models/matchmaking/registry.py:96`
  - **Problema:** StrategyFactory._clone_strategy ritorna la STESSA istanza (singleton dal registry) invece di clonarla. create_strategy(name, seed) inietta poi il contesto deterministico via set_context su quell'istanza condivisa, mutando _rng/_precomputed_schedule dell'oggetto globale registrato in bootstrap. Le esecuzioni concorrenti o successive senza seed riusano l'RNG seedato e la cache schedule contaminata.
  - **Scenario:** Un test/replay chiama matchmaking_service.run('random', gara, n, seed=42): la singola istanza RandomAntiRematchStrategy registrata globalmente riceve set_context(seed=42). Una generazione successiva 'in produzione' senza seed usa ancora quell'RNG/cache, producendo pairing non piu' casuali o uno schedule cache incoerente con la nuova gara.
  - **Verifica:** Bug reale e costruibile dal codice. `StrategyFactory._clone_strategy` (registry.py:111) ritorna la STESSA istanza, vanificando lo scopo dichiarato del metodo ("prevent state sharing between operations"). Le strategie NON sono stateless: `RandomAntiRematchStrategy.__init__` (random_anti_rematch.py:66-71) tiene stato mutabile `self._rng`, `self._precomputed_schedule`, `self._schedule_signature`, e `set_context` (linee 73-78) MUTA `self._rng = context.get_rng()` sull'istanza condivisa. In bootstrap.py:35 viene registrata UNA sola istanza globale nel modulo-level `_registry`.

Catena di chiamata c…
- **[correttezza] (CONFIRMED)** `models/matchmaking/strategies/amalfi.py:136`
  - **Problema:** AmalfiStrategy usa random.shuffle globale (righe 136, 204) e non implementa set_context. Poiche' StrategyFactory.create inietta il contesto solo se hasattr(strategy,'set_context'), il parametro seed di MatchmakingService.run viene silenziosamente ignorato per Amalfi: la promessa documentata di replay deterministico (registry/service docstring) non e' rispettata e l'uso di random globale non e' isolato dai test.
  - **Scenario:** Un direttore contesta un sorteggio Amalfi del primo round e si richiede il replay con seed fisso per riprodurlo. run('amalfi', gara, 1, seed=123) ignora il seed (nessun set_context) e usa random globale: l'ordine casuale non e' riproducibile, impossibile riprodurre/verificare il pairing contestato.
  - **Verifica:** Bug reale e costruibile dal codice. (1) amalfi.py usa random globale: `import random` (riga 2) e `random.shuffle(players)` a riga 136 (`_create_random_classification`) e riga 204 (`_create_campionato_classification`). (2) Ne' AmalfiStrategy ne' BaseStrategy definiscono `set_context`; solo random_anti_rematch.py:73 lo implementa. (3) StrategyFactory.create inietta il contesto solo `if context and hasattr(strategy, "set_context")` (registry.py:91-92): per Amalfi e' False, quindi `PairingContext(seed)` viene costruito (registry.py:186) e silenziosamente scartato. Amalfi non tocca mai `self._rng`/…
- **[correttezza] (PLAUSIBLE)** `models/individual_match/match_models.py:601`
  - **Problema:** _check_multi_set_completion assegna il vincitore a player2 in caso di parita' di set: il ramo 'else' copre sia player2>player1 sia player1==player2. Per match exact-sets (is_race_to_sets=False) il completamento puo' scattare in parita'
  - **Scenario:** Match multi-set con is_race_to_sets=False e match_distance=2 (creabile da proposta, is_race_to_sets passato via request.args in routes/individual_match/proposals.py:162). Punteggio set 1-1: _is_multi_set_complete ritorna True (total 2 >= 2), ma essendo player1_score == player2_score il ramo else imposta winner_id = player2_id assegnando erroneamente la vittoria al giocatore 2 invece di gestire il pareggio.
  - **Verifica:** Il difetto di codice esiste come descritto: in `models/individual_match/match_models.py:601-604`, `_check_multi_set_completion` usa `if self.player1_score > self.player2_score: winner=player1 else: winner=player2`, e il ramo else copre anche il caso di parità (player1_score == player2_score), assegnando erroneamente la vittoria a player2. Questo metodo È raggiungibile (chiamato da `_complete_set` a riga 753 ad ogni set completato), e per `is_race_to_sets=False` `_is_multi_set_complete` (riga 593) ritorna True quando la somma dei set raggiunge `match_distance`, quindi uno stato 1-1 con match_di…
- **[correttezza] (CONFIRMED)** `models/kpi/metrics_service.py:102`
  - **Problema:** get_activation_rate somma distinct(player1_id) e distinct(player2_id) come 'unique_players'. Gli utenti presenti in entrambi i ruoli sono contati due volte; il min(..., total_users) maschera l'overflow ma non corregge il numeratore, gonfiando il tasso di attivazione.
  - **Scenario:** Con 10 utenti tutti attivi, 8 come p1 e 6 come p2 (sovrapposti), somma=14 -> clampato a 10 -> 100% anche se alcuni non hanno mai giocato; oppure il rate risulta artificialmente alto rispetto al reale numero di giocatori distinti, contraddicendo la docstring '% of users who have played at least 1 match'.
  - **Verifica:** Bug reale in models/kpi/metrics_service.py:101-103. get_activation_rate() conta separatamente distinct(player1_id) (riga 87-92) e distinct(player2_id) (riga 94-99), poi li somma in `unique_players = min(users_with_matches + users_with_matches_p2, total_users)` (riga 102). Un utente che gioca sia come player1 (in alcune partite) sia come player2 (in altre) viene contato due volte. Non c'e' nessun vincolo che fissi un utente sempre come player1 o sempre come player2: in qualsiasi schema di matchmaking (round-robin, Amalfi, eliminazione) i giocatori alternano i ruoli, quindi la sovrapposizione e'…
- **[correttezza] (CONFIRMED)** `models/notification/services.py:101`
  - **Problema:** In create_notification (sotto @transactional) l'evento SSE emit_user_event con unread_count viene emesso PRIMA del commit. Se la transazione poi fallisce/rollback, il client ha gia ricevuto un badge count incrementato che non corrisponde allo stato persistito.
  - **Scenario:** create_notification aggiunge la notifica, calcola new_count via autoflush ed emette subito l'evento SSE (riga 101). Se un errore successivo nello stesso contesto transazionale (o un altro step della stessa unit-of-work che ingloba questa chiamata) causa rollback, la notifica non viene salvata ma il browser dell'utente ha gia aggiornato il contatore non letti a un valore piu alto: badge persistentemente sfasato fino al refresh/poll successivo. L'emissione side-effect (I/O) andrebbe fatta dopo il commit.
  - **Verifica:** Real ordering bug confirmed in models/notification/services.py:101. create_notification() is decorated with @transactional(domain="notification"); the actual commit happens only after the function returns (wrapper in models/transaction/manager.py:447-453 + context-manager __exit__ at lines 281-290). But emit_user_event(user_id, "notification", {"unread_count": new_count}) at line 101 runs BEFORE that commit. emit_user_event writes into the in-memory _events store (routes/sse.py:146-156, 89-117) which is served to polling clients via /poll/user/<id> — i.e. it is a client-visible side effect per…
- **[correttezza] (PLAUSIBLE)** `models/notification/models.py:243`
  - **Problema:** get_translated_content: il fallback nel except (KeyError/TypeError) ritorna _(title_template)/_(message_template) SENZA sostituzione dei parametri. Quando manca un param o c'e un mismatch di tipo, l'utente vede i placeholder grezzi tipo '+%(xp)d XP' / '%(name)s' nel testo della notifica.
  - **Scenario:** Una notifica gamification ha template 'achievement.unlocked' con message 'Hai ottenuto %(name)s (%(difficulty)s)! +%(xp)d XP' ma template_params salvati non contengono 'xp' (o 'xp' e una stringa non castabile a %d). La sostituzione _(message_template) % params solleva KeyError/TypeError, si entra nel except e si ritorna il template tradotto ma con i placeholder letterali: l'utente legge 'Hai ottenuto Trick Shot (...)! +%(xp)d XP'. Degrado visibile della UI invece di un messaggio sensato.
  - **Verifica:** Il difetto di codice è reale: in models/notification/models.py il blocco except (KeyError/TypeError) alle righe 243-249 ritorna `_(title_template)`/`_(message_template)` SENZA `% params`, quindi se la sostituzione fallisce l'utente vede placeholder grezzi (es. '%(name)s', '+%(xp)d XP') renderizzati direttamente in templates/player/notifications.html:87. Su questo punto il finding ha ragione.

PERÒ lo scenario specifico citato (notifica gamification 'achievement.unlocked' con 'xp' mancante o di tipo errato) NON è costruibile dai producer reali: in models/gamification/notification_handlers.py tu…
- **[correttezza] (PLAUSIBLE)** `models/playoff/services.py:128`
  - **Problema:** notify_qualified_players filtra notified_at=None (campo legacy/deprecated), ma start_playoff e _send_playoff_invitations popolano invited_at, non notified_at. Tutte le qualifiche PENDING restano con notified_at=None.
  - **Scenario:** Se decline_qualification (o un altro path) trova un sostituto e chiama notify_qualified_players(configuration_id), la query con notified_at=None restituisce TUTTI i PENDING (perche' nessuno ha notified_at valorizzato), reinviando notifiche/duplicati a giocatori gia' invitati invece del solo nuovo sostituto. Mix dei due campi temporali (invited_at vs notified_at deprecato) genera comportamento incoerente.
  - **Verifica:** Il nucleo tecnico del finding e' corretto: `notify_qualified_players` (services.py:125-129) filtra `status=PENDING, notified_at=None`, ma `notified_at` e' il campo deprecato (models.py:371 "Deprecated: use invited_at") e NON viene mai popolato: sia `start_playoff` (services.py:563,592,602) sia `_send_playoff_invitations` valorizzano solo `invited_at`. Quindi la query con `notified_at=None` matcherebbe TUTTI i PENDING, e il loop (services.py:134-135) reinvierebbe a tutti.

PERO' lo scenario concreto descritto ("decline_qualification trova un sostituto e chiama notify_qualified_players reinviand…
- **[correttezza] (CONFIRMED)** `models/dashboard/section_builders.py:209`
  - **Problema:** build_challenge_sections sovrascrive player_challenge_progress[user_id] ad ogni iterazione del loop su target_garas. Se l'utente e' iscritto a piu' gare con challenge, solo il progresso dell'ULTIMA gara sopravvive, mentre available_challenges aggrega correttamente TUTTE le gare -> incoerenza tra challenge mostrate e progresso disponibile.
  - **Scenario:** Player iscritto a gara A e gara B, entrambe con challenge attive. available_challenges contiene le challenge di A e B, ma player_challenge_progress[user_id] viene riscritto e mantiene solo il progresso di B (ultima iterata). Nel componente _player_challenges_dashboard.html (che legge vm.player_challenge_progress.get(current_user.id)) il progresso delle challenge di gara A risulta assente/errato.
  - **Verifica:** Bug reale in models/dashboard/section_builders.py:209. In build_challenge_sections il loop su target_garas (192-209) accumula correttamente le challenge di TUTTE le gare con available_challenges.extend(gara_challenges) (202), ma il progresso viene scritto con player_challenge_progress[user_id] = progress (209), sovrascrivendo l'unica chiave user_id ad ogni iterazione. get_user_gara_challenge_progress (gara_challenge_service.py:309-365) ritorna un dict con "challenges" limitato alla singola gara passata (320: get_gara_challenges(gara_id)). Quindi dopo il loop, player_challenge_progress[user_id]…
- **[correttezza] (PLAUSIBLE)** `models/exam/models.py:261`
  - **Problema:** ExamAttempt.get_progress() conta come 'completate' solo le challenge con score IS NOT NULL. Ma ExamChallengeResult.complete_challenge() permette di completare con score=None e solo passed impostato (challenge pass/fail). Una challenge pass/fail completata non viene mai conteggiata, quindi progress['is_complete'] resta False e l'exam non si auto-completa.
  - **Scenario:** Exam che contiene una challenge pass/fail: il director la completa con complete_exam_challenge(passed=True) senza score. get_progress non la conta -> is_complete=False -> complete_exam() non viene mai chiamato automaticamente in complete_exam_challenge (services line 161). L'attempt resta eternamente non completato a meno di chiamata manuale a complete_exam_attempt.
  - **Verifica:** The logic flaw is real and exactly as described. In models/exam/models.py:307-318, ExamChallengeResult.complete_challenge() assigns self.score = score and self.passed = passed with NO validation/normalization — so it accepts score=None, passed=True (a pass/fail completion). Then get_progress() at models.py:261-263 counts as "completed" only results where ExamChallengeResult.score.isnot(None). Consequently a pass/fail-completed challenge is never counted, completed_challenges < total_challenges, is_complete stays False, and the auto-complete branch in services.py:161 (`if progress["is_complete"…
- **[correttezza] (PLAUSIBLE)** `models/exam/services.py:121`
  - **Problema:** start_exam_attempt() ha una TOCTOU race: controlla l'esistenza di un attempt non completato con una query e poi ne crea uno nuovo se assente. Non c'e' un vincolo UNIQUE su (user_id, exam_id, completed=False), quindi due richieste concorrenti dello stesso utente possono creare due attempt attivi duplicati.
  - **Scenario:** Uno studente fa doppio click o due tab partono insieme: entrambe le richieste passano il check 'existing_attempt is None' prima che l'altra committi, e vengono creati due ExamAttempt attivi con i relativi ExamChallengeResult duplicati, falsando statistiche e progress.
  - **Verifica:** The TOCTOU pattern described is technically real in the code. In models/exam/services.py:121-137, start_exam_attempt() does check-then-create: it queries for an active attempt (lines 121-123 filter_by completed=False), returns it if found (125-126), else creates a new ExamAttempt + ExamChallengeResult rows (129-137). I confirmed in models/exam/models.py that ExamAttempt has NO UNIQUE constraint guarding this invariant: __table_args__ exists only on ExamChallenge (uq_exam_challenge, uq_exam_order); ExamAttempt has none. So two concurrent transactions for the same (user_id, exam_id) can both pas…
- **[correttezza] (PLAUSIBLE)** `models/transaction/manager.py:138`
  - **Problema:** Stato condiviso non thread-safe sull'istanza globale transaction_manager: _next_transaction_id (incrementato in generate_transaction_id) e _metrics_history (append + slicing alle linee 322-326) sono attributi di istanza mutati senza lock da piu' thread. current_transaction e' thread-local, ma questi non lo sono. In un server WSGI multithread (PythonAnywhere) sono soggetti a race condition.
  - **Scenario:** Due request concorrenti entrano in transazioni nello stesso momento: '_next_transaction_id += 1' non e' atomico -> ID transazione duplicati; append/troncamento concorrenti su _metrics_history possono perdere elementi o corrompere la lista, falsando get_performance_summary. Non corrompe il DB ma produce metriche/log inaffidabili e ID non univoci.
  - **Verifica:** The data race is real at the code level: in models/transaction/manager.py, `self._next_transaction_id += 1` (line 141) and the `_metrics_history.append(...)` + slice-reassignment (lines 322-326) mutate instance state of the module-global `transaction_manager` (line 434) without any lock, while `current_transaction` is correctly thread-local but these two are not. PythonAnywhere serves WSGI multithreaded by default, so concurrent `@transactional` calls are realistic. However, the impact claimed in the finding is overstated: (1) the generated `transaction_id` is used ONLY for log strings and sav…
- **[correttezza] (CONFIRMED)** `models/player/history_service.py:233`
  - **Problema:** I filtri data in _apply_match_filters usano Gara.date >= date_from / <= date_to su una Gara outerjoin'd. Per i match standalone (gara_id NULL -> Gara.date NULL) la condizione e' NULL, quindi falsy: applicando un qualsiasi filtro data tutti i match individuali standalone spariscono dallo storico, anche se la loro created_at rientra nell'intervallo. Lo stesso problema vale per il filtro discipline (riga 202-211): un match standalone con Match.discipline NULL viene escluso perche' ricade sul ramo Gara.discipline che e' NULL.
  - **Scenario:** Un utente con match individuali standalone (gara_id NULL) imposta date_from/date_to per restringere il periodo. Tutti i suoi match standalone scompaiono dai risultati indipendentemente dalla data reale, perche' Gara.date e' NULL e la comparazione SQL produce NULL (escluso). Il giocatore vede uno storico incompleto e statistiche sottostimate.
  - **Verifica:** Bug reale e costruibile in models/player/history_service.py::_apply_match_filters.

La base query (righe 107-141 e 153-189) e' deliberatamente costruita per INCLUDERE i match standalone (gara_id NULL) tramite outerjoin su Gara, e l'ordinamento usa .nullslast() (righe 147, 191) — prova esplicita che gli autori si aspettano righe con Gara.date NULL nel risultato. Il commento alle righe 105/153 conferma "outerjoin for Gara to include standalone matches (gara_id = NULL)".

FILTRO DATA (bug certo, righe 233-236):
  if filters.date_from: query.filter(Gara.date >= filters.date_from)
  if filters.date…
- **[correttezza] (CONFIRMED)** `routes/gamification/dashboard.py:196`
  - **Problema:** limit = min(int(request.args.get('limit', 20)), 100) chiama int() su un parametro di query grezzo senza type=int ne' try/except. Su una route PUBBLICA (nessun login_required) un valore non numerico provoca ValueError non gestito. Idem leaderboard_type_str non e' validato contro type_map (che peraltro e' costruito e mai usato: dead code).
  - **Scenario:** Un visitatore (o un bot/crawler) apre /gamification/leaderboards?limit=abc oppure ?limit=-> int('abc') solleva ValueError non catturato -> HTTP 500. Essendo route anonima e indicizzabile, e' un DoS/errore banalmente innescabile dall'esterno. type=int (come fatto altrove con request.args.get(..., type=int)) lo eviterebbe restituendo None/default.
  - **Verifica:** Bug reale e confermato a routes/gamification/dashboard.py:196. La riga `limit = min(int(request.args.get("limit", 20)), 100)` chiama `int()` su un parametro di query grezzo: un valore non numerico (es. ?limit=abc) solleva ValueError non gestito → HTTP 500. La route /leaderboards non ha @login_required (la docstring dice esplicitamente "public route, no login required") e nessun try/except la protegge. Il pattern idiomatico del progetto è `request.args.get(..., type=int)` (usato in routes/dashboard.py:16-17, routes/CLAUDE.md:217, ecc.), che restituirebbe None/default su input non valido evitand…
- **[correttezza] (CONFIRMED)** `routes/challenge.py:336`
  - **Problema:** In create_x_replacement (e in complete_attempt) `data.get("challenge_id", type=int)` fallisce con TypeError quando la richiesta e' JSON, perche' data e' un dict. except ValueError non lo cattura -> 500.
  - **Scenario:** POST JSON a /challenge/x-replacement/<gara>/<round> con {"challenge_id": 3}. request.is_json True -> data dict -> data.get('challenge_id', type=int) solleva TypeError non gestito -> 500 invece del 400/redirect previsto.
  - **Verifica:** Real bug at routes/challenge.py:336. Line 330 sets `data = request.get_json() if request.is_json else request.form`. For a JSON request `data` is a plain dict, and line 336 calls `data.get("challenge_id", type=int)`. Python's `dict.get()` does not accept keyword arguments — I verified it raises `TypeError: dict.get() takes no keyword arguments` (the `type=int` kwarg is a Werkzeug MultiDict/`request.form` feature, not a dict feature). The handler's only except clause is `except ValueError` (line 352), which does NOT catch `TypeError`, so the request results in an unhandled 500 instead of the in…
- **[correttezza] (CONFIRMED)** `routes/rating.py:103`
  - **Problema:** calculate_handicap usa `data.get("rule_id", type=int)` ma su path JSON `data` e' un dict: TypeError non catturato da `except ValueError` -> 500.
  - **Scenario:** POST JSON a /handicap/calculate con {"player1_id":1,"player2_id":2}. data e' dict, data.get('rule_id', type=int) -> TypeError 'dict.get() takes no keyword arguments' -> 500 anziche' risposta JSON di errore controllata.
  - **Verifica:** In routes/rating.py:98 `data = request.get_json() if request.is_json else request.form`. On the JSON path `data` is a plain dict, and line 103 `rule_id=data.get("rule_id", type=int)` passes a `type=` keyword to `dict.get`, which raises `TypeError: dict.get() takes no keyword arguments` (verified by running it). The handler's only catch is `except ValueError` (line 113), which does NOT catch `TypeError`, so a JSON POST to /handicap/calculate returns an uncaught 500 instead of the intended `{"success": False, ...}, 400`. The path is reachable: int(data["player1_id"]) and int(data["player2_id"]) …
- **[architettura] (CONFIRMED)** `models/events/notification_handlers.py:119`
  - **Problema:** Tutti i titoli/messaggi di notifica sono stringhe italiane hard-coded senza _() né template_key. NotificationService supporta i18n via template_key/template_params (servizio tradotto a display-time), ma questi handler salvano testo grezzo. Viola la convenzione 'tutte le stringhe utente in i18n'.
  - **Scenario:** Un utente con interfaccia in inglese riceve notifiche (richiesta direttore, proposta partita, iscrizioni aperte, ecc.) sempre in italiano, perché il testo è persistito grezzo senza chiave di traduzione. La piattaforma è dichiaratamente i18n-first.
  - **Verifica:** Il finding è reale. Tutti gli handler in models/events/notification_handlers.py chiamano NotificationService.create_notification(...) passando title=/message= come stringhe italiane grezze e NON passano mai template_key/template_params. Esempi: line 120-121 (title="Nuova Richiesta Direttore", message=f"L'utente {event.username} ha richiesto di diventare direttore."), line 142-143, line 176-184, line 214-225, line 265-266, line 294-296, line 335-336, ecc.

L'infrastruttura i18n display-time esiste ed è funzionante: models/notification/services.py (line 43-44, 76-83) accetta template_key/templat…
- **[performance] (CONFIRMED)** `models/campionato/statistics_service.py:443`
  - **Problema:** partition_campionati_by_status chiama c.get_status() per ogni campionato; get_status() delega a compute_campionato_status che, per i campionati terminated con playoff, esegue query PlayoffConfiguration + PlayoffTournament (una per config) per campionato. Invocata su tutta la lista candidati della homepage/dashboard -> N+1 nascosto dentro il partizionamento.
  - **Scenario:** Dashboard/homepage con molti campionati terminati con configurazioni playoff: ogni render itera tutti i campionati e per ciascuno emette query playoff aggiuntive, scalando linearmente col numero di campionati storici (mai limitato prima del partizionamento).
  - **Verifica:** N+1 reale e costruibile dal codice. La homepage carica TUTTI i campionati non eliminati senza limite (models/campionato/homepage_service.py:47-51, query .all() su Campionato.query.filter_by(is_deleted=False)) e passa l'intera lista a partition_campionati_by_status. La funzione (models/campionato/statistics_service.py:442-443) itera ogni candidato chiamando c.get_status(), che delega a compute_campionato_status (riga 344). Per ogni campionato con terminated_at e playoff configs, compute_campionato_status esegue PlayoffConfiguration.query.filter_by(...).all() (riga 365-367) PIU' un PlayoffTourna…
- **[performance] (CONFIRMED)** `models/campionato/statistics_service.py:208`
  - **Problema:** _aggregate_player_totals, per i sistemi a punti (non amalfi/random), ri-itera la lista garas e ri-esegue per ogni gara la stessa query RoundClassification gia' eseguita nel primo loop (righe 147-152), raddoppiando le query identiche per calcolare i punti che potevano essere derivati nel primo passaggio.
  - **Scenario:** Campionato con classification system a punti e molte gare: ogni calcolo di classifica generale esegue 2x le query RoundClassification per gara; chiamato anche due volte (classifica corrente + precedente) in calculate_general_classification, quadruplicando il costo.
  - **Verifica:** Real, code-constructible redundancy in models/campionato/statistics_service.py::_aggregate_player_totals. The first loop (lines 144-189) already runs `db.session.query(RoundClassification).filter_by(gara_id=gara.id, round_number=final_round).order_by(position).all()` per gara (lines 147-152). For points-based campionati (campionato_type not in AMALFI/RANDOM), the second loop (lines 208-215) re-iterates the SAME `garas` and re-executes the byte-identical query (lines 210-214) just to compute position_points. The data needed (classification.position, used at line 219) is already present on the o…
- **[performance] (CONFIRMED)** `models/kpi/tracker.py:37`
  - **Problema:** FeatureTracker._session_tracked e' un set a livello di classe che accumula chiavi 'feature:date:session_id' (session_id = uuid nuovo per ogni request) e non viene mai svuotato: reset_session() non e' chiamato da nessuna parte del codebase. Memory leak monotono nel worker long-running, e stato mutabile condiviso tra thread/worker.
  - **Scenario:** In produzione (worker persistente) ogni request genera un nuovo uuid in g; ogni primo track di una feature aggiunge una chiave permanente al set. Dopo milioni di request il set cresce illimitato consumando memoria fino all'OOM/riavvio, senza alcun beneficio (le chiavi vecchie con uuid passati non saranno mai piu' consultate).
  - **Verifica:** Bug reale e costruibile dal codice. `FeatureTracker._session_tracked` (models/kpi/tracker.py:37) è un set a livello di classe. In `_track_impl` (riga 75) viene aggiunta la chiave `feature:date:session_id`, dove `session_id` è un `str(uuid.uuid4())[:8]` rigenerato ad ogni request (riga 94, salvato solo in `g` che è request-scoped). Quindi ogni request che esegue un track aggiunge almeno una chiave nuova e permanente al set. L'unico metodo che lo svuota, `reset_session()` (riga 104), non è chiamato da nessuna parte: grep su tutto il repo (incl. app.py after_request e i test) trova solo la sua de…
- **[performance] (CONFIRMED)** `models/kpi/community_service.py:43`
  - **Problema:** get_director_performance esegue query per ogni director dentro il loop (User.query.get, Gara.query.filter_by, due DirectorAssignment.query, accesso lazy ca.campionato.gare) prima della re-query con selectinload. Classico N+1 che scala col numero di director e di campionati assegnati.
  - **Scenario:** Con N director, ciascuno con M assignment di campionato, l'endpoint /admin/kpi esegue dell'ordine di N*(4 + M) query (User.get, gare per director, campionato/gara assignment, e un SELECT per ogni ca.campionato.gare). Su una community con decine di director il caricamento della dashboard KPI diventa lento e carica inutilmente il DB.
  - **Verifica:** Bug reale (performance N+1) costruibile dal codice. In `models/kpi/community_service.py::get_director_performance`, il loop `for d_id in director_ids:` (linea 43) esegue per OGNI director una serie di query separate: `User.query.get(d_id)` (l.44), `Gara.query.filter_by(director_id=d_id).all()` (l.48), `DirectorAssignment.query.filter_by(... entity_type="campionato")` (l.50-52), `DirectorAssignment.query.filter_by(... entity_type="gara")` (l.58-60), e una re-query con selectinload (l.78-82). In aggiunta, dentro i loop annidati accede a relazioni lazy: `ca.campionato` e `ca.campionato.gare` (l.5…
- **[performance] (PLAUSIBLE)** `models/playoff/services.py:865`
  - **Problema:** get_user_playoff_history fa N+1 (qualification.configuration.campionato.name dentro il loop, due lazy-load per riga) e dereferenzia configuration.campionato senza None-check: se la qualifica punta a una config il cui campionato e' stato cancellato/anonimizzato il dereference esplode.
  - **Scenario:** Un utente con molte qualifiche apre la propria cronologia playoff: per ogni qualifica partono 2 query lazy (configuration, campionato). Inoltre se campionato e' None (FK con ondelete o record orfano), 'qualification.configuration.campionato.name' alla riga 865 solleva AttributeError su None, rompendo l'intera pagina history.
  - **Verifica:** Il finding mescola due affermazioni, una vera-ma-minore e una falsa.

1) N+1 (nucleo performance): VERO come fatto di codice. A models/playoff/services.py:865-866 il loop accede a `qualification.configuration.campionato.name` e `qualification.configuration.name`. La query a riga 859 (`PlayoffQualification.query.filter_by(user_id=user_id).all()`) non ha joinedload/selectinload, e sia `configuration` (su PlayoffQualification) sia `campionato` (`db.relationship("Campionato")` a models/playoff/models.py:94) sono relazioni lazy="select" di default → 2 lazy-load per riga. Quindi l'N+1 esiste davvero…
- **[performance] (PLAUSIBLE)** `models/location/services.py:355`
  - **Problema:** suggest_locations_for_match chiama get_location_statistics(hall.id) dentro il loop su user_locations. Ogni get_location_statistics esegue almeno 2 query (count UserLocationAvailability + count IndividualMatch) piu' il session.get del hall: query N+1 sull'hot path di suggerimento, eseguite per ogni sede dell'utente.
  - **Scenario:** Un utente con 15 sedi disponibili invoca suggest_locations_for_match: vengono eseguite ~45+ query (3 per sede) piu' quelle iniziali, anche se molte non verranno mai mostrate. Con utenti che hanno molte sedi questo degrada visibilmente la pagina dei suggerimenti.
  - **Verifica:** The N+1 pattern is real and accurately described at the code level. In models/location/services.py, suggest_locations_for_match loops over user_locations (line 315) and calls LocationService.get_location_statistics(hall.id) inside the loop (line 355). Each get_location_statistics call runs 2-3 queries: db.session.get(BilliardHall,...) (line 266), UserLocationAvailability.query...count() (line 271), and IndividualMatch.query.filter_by(...).count() (line 278). So a user with N available halls triggers ~2-3N queries, matching the finding. This is a genuine query-inside-loop N+1 of the kind the gu…
- **[performance] (CONFIRMED)** `models/player/history_service.py:399`
  - **Problema:** _calculate_gara_stats esegue una query RoundClassification separata dentro il loop su ogni gara (N+1). Inoltre get_gara_history fa joinedload(Gara.round_classifications) (riga 340) ma quel caricamento eager viene completamente ignorato qui: si interroga il DB di nuovo invece di usare la relazione gia' caricata.
  - **Scenario:** Un giocatore con storico di 200 gare apre la pagina storico: oltre alla query principale (gia' con eager-load inutile delle round_classifications), partono 200 query aggiuntive a RoundClassification, una per gara. Su SQLite con concorrenza questo degrada visibilmente il tempo di risposta e spreca il joinedload.
  - **Verifica:** Real N+1 + wasted eager-load. In models/player/history_service.py, get_gara_history adds joinedload(Gara.round_classifications) (line 340) and materializes the full result set via all_gare = query.all() (line 355). Then _calculate_gara_stats loops `for gara in gare` and, at lines 399-407, issues a fresh `db.session.query(RoundClassification).filter(gara_id==gara.id, user_id==..., round_number==gara.current_round).first()` per iteration. The relationship loaded at line 340 (backref defined in models/classification/models.py:91) is never consulted, so the eager-load is pure wasted work AND a sep…

### Severità: LOW (22)

- **[correttezza] (PLAUSIBLE)** `models/matchmaking/strategies/direct_elimination.py:41`
  - **Problema:** Incoerenza nel filtro iscrizioni: _validate_strategy_specific (righe 41-45) filtra solo is_withdrawn, mentre _generate_first_round_pairings (righe 101-105) filtra anche is_waitlist. Il player_count usato per calcolare required_rounds nella validazione include gli iscritti in waitlist, sovrastimando i round necessari e potendo generare un errore di validazione spurio (o sottostimare i bye effettivi).
  - **Scenario:** Gara a eliminazione con alcune iscrizioni in waitlist (parita'/capienza). La validazione conta i waitlist nel player_count e calcola required_rounds piu' alto del reale: se rounds_count e' tarato sui giocatori attivi, validate() segnala erroneamente 'requires N rounds' e blocca l'avvio della gara.
  - **Verifica:** Real, code-confirmed inconsistency. In _validate_strategy_specific (models/matchmaking/strategies/direct_elimination.py:42-45) player_count filters only is_withdrawn, while _generate_first_round_pairings (lines 101-105) filters BOTH is_withdrawn AND is_waitlist. So required_rounds = ceil(log2(player_count)) at line 48-50 is computed over an inflated count that includes waitlisted inscriptions, whereas the actual bracket is built only on active (non-waitlist) players. The project explicitly supports waitlisted inscriptions (parity/capacity; see models/matchmaking/CLAUDE.md OddNumberPolicy.NO) a…
- **[correttezza] (PLAUSIBLE)** `models/classification/score_aggregator.py:48`
  - **Problema:** aggregate_round_scores filtra i match con la lista letterale Match.status.in_(['completed','validated']) invece del predicato tipo-safe MatchStatus.is_finished, in contrasto con la convenzione del progetto (models/CLAUDE.md) gia adottata in aggregate_campionato_scores (riga 110) e _count_gare_played. Un eventuale rinomina/typo di uno stato passa silenziosamente e fa sparire i match dall'aggregazione.
  - **Scenario:** Se in futuro uno stato finished viene rinominato/aggiunto (es. 'auto_validated'), aggregate_round_scores continua a usare la lista hard-coded e ignora quei match, producendo classifiche di round con punteggi mancanti senza alcun errore.
  - **Verifica:** Confermato come incoerenza reale di convenzione, ma a impatto nullo oggi (severita' low giustificata). A models/classification/score_aggregator.py:48 il filtro usa la lista letterale `Match.status.in_(["completed", "validated"])`, mentre il metodo gemello `aggregate_campionato_scores` alla riga 110 usa `MatchStatus.is_finished(m.status)`. Ho verificato in models/status_enum.py:114-115 che `MatchStatus.is_finished`/`finished_statuses` restituisce esattamente `("completed", "validated")`, quindi i due path sono OGGI semanticamente equivalenti: nessun match viene perso, nessun bug runtime attuale…
- **[correttezza] (PLAUSIBLE)** `models/classification/gara_classification.py:94`
  - **Problema:** visible_user_ids_for_gara dichiara nel commento 'iscritti non ritirati' ma include TUTTI gli Inscription del gara senza filtrare is_withdrawn/is_waitlist; inoltre interroga User con deleted_at.isnot(None) bypassando il filtro di soft-delete di sessione. La funzione e esportata in services.py ma attualmente non ha chiamanti (dead code) quindi il rischio e latente.
  - **Scenario:** Se un domani questa helper viene usata per filtrare la classifica visibile, includerebbe giocatori ritirati e in lista d'attesa (contro l'intento dichiarato), mostrando in classifica utenti che non dovrebbero comparire.
  - **Verifica:** Real but latent. At models/classification/gara_classification.py:94-104, visible_user_ids_for_gara has a comment "iscritti non ritirati" yet line 96-99 queries ALL Inscription for the gara with no is_withdrawn=False / is_waitlist=False filter — contradicting both its own comment and the project's documented active-inscription pattern (models/CLAUDE.md "Filtering Active Inscriptions"; Inscription.get_active_inscriptions at competition/models.py:720 filters both flags). So withdrawn AND waitlist users would be included if this helper were ever used to filter visible classification, showing playe…
- **[correttezza] (PLAUSIBLE)** `models/individual_match/proposal_service.py:599`
  - **Problema:** express_interest_in_open_invitation ritorna invitation.id senza prima fare db.session.flush(): l'oggetto e' solo aggiunto alla session, quindi id e' None al momento del return
  - **Scenario:** Un client chiama express_interest_in_open_invitation e usa il campo 'invitation_id' della risposta per azioni successive (es. revoca/accettazione mirata). Il valore e' None perche' la PK viene assegnata solo al flush/commit, mentre l'accesso a .id su un oggetto pending non scatena autoflush. Il chiamante riceve invitation_id=None e non puo' referenziare l'invito appena creato.
  - **Verifica:** The technical defect is REAL and I verified it empirically. In models/individual_match/proposal_service.py, line 595 does db.session.add(invitation) and line 599 builds the return dict with "invitation_id": invitation.id. The @transactional decorator (models/transaction/manager.py:453) executes func() and captures its return value INSIDE the with-block; the commit only happens AFTER func returns (line 283). No flush occurs in between, and accessing a PK on a pending object does not trigger autoflush. I confirmed this by running the app: inv.id is None before flush, 13 after flush. So the retur…
- **[correttezza] (CONFIRMED)** `models/kpi/tracker.py:65`
  - **Problema:** Il tracker usa date.today() (data locale del server) come for_date, mentre tutte le metriche aggregano su utc_now()/func.date(updated_at) (UTC). Violazione della convenzione UTC del progetto: il giorno di tracking puo' non coincidere col giorno usato dalle query metriche.
  - **Scenario:** Server con offset locale (es. UTC+2): un evento tracciato alle 23:30 UTC (01:30 locale) viene aggregato nel KpiFeatureUsage del giorno locale successivo, mentre i match dello stesso istante finiscono nel giorno UTC precedente; i grafici feature-usage risultano sfasati di un giorno rispetto agli altri KPI nelle ore a cavallo della mezzanotte.
  - **Verifica:** Bug reale e costruibile dal codice. In models/kpi/tracker.py:65 il tracker fa `for_date = date.today()`, che restituisce la data LOCALE del server (Python `date.today()` usa l'ora locale). Lo stesso vale per KpiFeatureUsage.get_or_create (models.py:38) e KpiDailySnapshot.get_or_create (models.py:116). Per contro, le metriche aggregano per data UTC: in metrics_service.py le query usano `func.date(Match.updated_at)` (righe 236/256/260/264), `func.date(User.created_at)` (38/60/64/68) e `func.date(XPTransaction.created_at)` (300) su timestamp che per convenzione di progetto sono naive-UTC (default…
- **[correttezza] (PLAUSIBLE)** `models/notification/models.py:331`
  - **Problema:** is_in_quiet_hours confronta quiet_hours_start/end (orari impostati dall'utente, presumibilmente ora locale) con utc_now().time(). La finestra 'silenzio' viene valutata in UTC anziche in ora locale italiana (UTC+1/+2), sfasando la finestra di 1-2 ore.
  - **Scenario:** Un utente configura quiet hours 22:00-08:00 pensando all'ora italiana. can_send_notification valuta now=utc_now().time(): alle 21:30 locali (19:30 UTC d'estate) la notifica passa anche se l'utente la voleva silenziata, e alle 06:30 locali (04:30 UTC) idem ai bordi. La feature non risulta cablata a UI/route attive (latente), ma quando verra esposta produrra silenziamenti sbagliati ai bordi della finestra.
  - **Verifica:** Bug tecnicamente reale ma latente. A models/notification/models.py:331, is_in_quiet_hours() fa `now = utc_now().time()` (UTC naive) e lo confronta con quiet_hours_start/end che set_user_preference() (services.py:302-313) popola da stringa "HH:MM" via datetime.strptime senza conversione di fuso. Dato che la convenzione del progetto e' UTC server + display locale (|datetime_local, UTC+1/+2), un utente che imposta "22:00" intende ora italiana ma il confronto avviene in UTC: la finestra di silenzio risulta sfasata di 1-2 ore ai bordi. La feature E' su un path live (can_send_notification -> is_in_q…
- **[correttezza] (CONFIRMED)** `models/rating/rating_service.py:78`
  - **Problema:** update_user_rating accetta i parametri `confidence` e `external_id` ma `confidence` viene completamente ignorato (non passato a update_player_rating, che a sua volta non lo gestisce). Il campo confidence sul modello PlayerRating (models.py:97) non viene mai valorizzato da questo path.
  - **Scenario:** Un chiamante imposta update_user_rating(..., confidence=0.9) aspettandosi che la confidence venga persistita; il valore viene scartato silenziosamente e PlayerRating.confidence resta NULL. Logica a valle che si fida di confidence (es. freshness/affidabilita') legge sempre None.
  - **Verifica:** Real bug, fully constructible from the code. In models/rating/rating_service.py, update_user_rating (lines 78-92) declares a `confidence: float = 0.5` parameter but its body (lines 86-92) calls update_player_rating(user_id, rating_system, new_rating, verified, external_id) WITHOUT passing confidence. update_player_rating (lines 271-301) has no confidence parameter and never assigns rating.confidence in either the update branch (283-289) or the create branch (291-299). PlayerRating.confidence is a real nullable Float column (models/rating/models.py:97, comment "Confidence level (0.0-1.0)"), so …
- **[correttezza] (CONFIRMED)** `models/dashboard/section_builders.py:145`
  - **Problema:** `user_locations.union(played_locations)` e' un no-op: set.union restituisce un nuovo set e non muta user_locations; il risultato e' scartato. Inoltre user_locations non viene usato altrove (le opportunities derivano da proposals.get('available')) -> codice morto/intenzione non realizzata di includere le location in cui l'utente ha gia' giocato.
  - **Scenario:** Il commento 'Also include locations where user has played before' suggerisce che le played_locations dovessero ampliare il set di location per calcolare le opportunita'. Di fatto non accade nulla: la logica e' inerte. Se in futuro qualcuno filtra opportunities per user_locations, le location dei match gia' giocati verrebbero silenziosamente escluse.
  - **Verifica:** In models/dashboard/section_builders.py line 145 `user_locations.union(played_locations)` is a no-op: set.union returns a new set without mutating user_locations, and the return value is discarded (not assigned). The comment on line 142 "Also include locations where user has played before" describes an intention never realized. Confirmed that neither `user_locations` (built lines 135-140) nor `played_locations` (line 143) is read anywhere after line 145: `opportunities` (line 148) derives solely from `proposals.get("available", [])` and the return dict (lines 150-154) never references the loca…
- **[correttezza] (PLAUSIBLE)** `models/location/services.py:371`
  - **Problema:** record_match_at_location fa fuzzy matching con BilliardHall.name.ilike('%location_name%') e prende il primo risultato. Con nomi sovrapposti (es. 'Sala Roma' e 'Sala Roma 2') o location_name molto corto, puo' associare il match alla sede sbagliata; inoltre se location_name e' vuoto/spazi diventa '%%' e matcha un venue arbitrario.
  - **Scenario:** Un IndividualMatch ha location='Roma'; esistono 'Biliardo Roma Nord' e 'Biliardo Roma Sud'. record_match_at_location incrementa matches_played_here sulla prima sede restituita dall'ilike (ordine non deterministico), attribuendo l'esperienza alla sede errata e falsando i punteggi di suggest_locations_for_match.
  - **Verifica:** Le tre criticità tecniche descritte sono reali nel testo del codice a models/location/services.py:370-372: `BilliardHall.name.ilike(f"%{location_name}%")` fa substring match e `.first()` senza ORDER BY restituisce una riga arbitraria/non deterministica; con `location_name` vuoto o di soli spazi il pattern diventa `%%`/`% %` e matcha un venue qualsiasi; con nomi sovrapposti ('Roma Nord'/'Roma Sud', location='Roma') attribuisce il match alla sede sbagliata. L'effetto, se eseguito, è gonfiare `matches_played_here` (bonus cap +5 in suggest_locations_for_match), quindi nessuna corruzione dati né pr…
- **[correttezza] (CONFIRMED)** `models/challenge/models.py:148`
  - **Problema:** get_statistics chiama 'median_score' ma calcola sorted(scores)[len(scores)//2], che per un numero pari di elementi restituisce l'elemento superiore-centrale (non la media dei due centrali) e per len pari sceglie l'indice piu' alto. Il valore restituito non e' la mediana statistica.
  - **Scenario:** Due tentativi con score [2, 8]: len//2 = 1 -> ritorna 8 come 'median_score' invece di 5. L'admin vede una mediana distorta verso l'alto su dataset con conteggio pari; con outlier la statistica mostrata e' fuorviante.
  - **Verifica:** models/challenge/models.py:148 computes `median_score = sorted(scores)[len(scores) // 2]`, which is not the statistical median for even-length datasets. The docstring (line 116) and return key (line 170) both call it "median_score". For an even count of completed attempts it returns the upper-middle element rather than the average of the two central elements: with scores [2, 8], len//2 = 1 → returns 8 instead of 5. This biases the reported median upward whenever there is an even number of completed attempts (total_attempts == 0 is already guarded at line 134, and odd counts happen to be correc…
- **[correttezza] (PLAUSIBLE)** `models/shared/email_service.py:32`
  - **Problema:** _get_sender ha un except ImportError (riga 32) per current_app.config.get(...), ma quella chiamata non puo' mai sollevare ImportError. L'except corretto per uso fuori dall'application context e' RuntimeError (gia' presente alla riga 34). Il ramo ImportError e' dead code che maschera intenti errati.
  - **Scenario:** Non causa malfunzionamenti diretti (il ramo non viene mai eseguito), ma e' indicativo di un fraintendimento: se in futuro current_app sollevasse un altro errore in un contesto non previsto, il fallback non scatterebbe. E' dead code da rimuovere/correggere.
  - **Verifica:** Il finding è fattualmente corretto ma è un nit di code-cleanliness, non un bug di correttezza con effetto runtime. A riga 31 `current_app.config.get(...) or os.environ.get(...) or DEFAULT`: `current_app` è un Flask LocalProxy che fuori dall'application context solleva RuntimeError (già gestito a riga 34), mentre `dict.get`/`os.environ.get` non sollevano mai ImportError. Gli import (`os` riga 6, `current_app` riga 9) sono a livello modulo e già risolti quando il metodo gira, quindi il ramo `except ImportError` a riga 32 è effettivamente irraggiungibile. È dead code innocuo: entrambi i rami (32 …
- **[correttezza] (CONFIRMED)** `models/player/history_service.py:218`
  - **Problema:** Il filtro opponent_id in _apply_match_filters confronta solo Match.player1_id/player2_id, ma la query base include anche i match trio (is_trio=True) dove i partecipanti reali stanno su TrioMatch.player1/2/3_id e player1_id/player2_id del Match possono non coincidere. Filtrando per avversario, i match trio contro quell'avversario vengono silenziosamente esclusi dallo storico filtrato e dalle relative statistiche.
  - **Scenario:** Un utente filtra lo storico per un avversario specifico con cui ha disputato match a trio: questi match trio non compaiono nei risultati ne' nelle statistiche aggregate, perche' il filtro guarda solo le posizioni 2-player del Match e ignora le posizioni del TrioMatch. Il giocatore crede di non aver mai affrontato quell'avversario in trio.
  - **Verifica:** Bug reale e costruibile. In `models/player/history_service.py` la query base di `get_match_history` include correttamente i match a trio tramite outerjoin su `TrioMatch` e OR sulle 3 posizioni `TrioMatch.player1/2/3_id` (righe 124-132). Ma il filtro avversario in `_apply_match_filters` (righe 218-230) confronta SOLO `Match.player1_id`/`Match.player2_id`. Per i match trio, alla creazione (round_creation.py:191-206 e match_service.py) `Match.player1_id=p0`, `Match.player2_id=p1` contengono solo 2 dei 3 giocatori, mentre il terzo (p2) sta unicamente su `TrioMatch.player3_id` e NON compare affatto…
- **[architettura] (CONFIRMED)** `models/classification/campionato_classification.py:67`
  - **Problema:** update_campionato_classification impila @transactional (esterno) + @cached + @optimized_query, dove sia @cached sia @optimized_query implementano caching. Oltre alla doppia cache ridondante, con un cache-hit il decoratore @transactional resta il piu esterno e apre comunque una transazione per restituire un risultato cachato (lavoro inutile su una read).
  - **Scenario:** Letture ripetute della classifica campionato aprono una transazione DB anche quando il risultato e gia in cache, e due livelli di cache (con TTL/tag diversi) possono dare invalidazioni incoerenti tra loro su modifiche concorrenti.
  - **Verifica:** Le due affermazioni del finding sono entrambe verificabili dal codice in models/classification/campionato_classification.py:66-74.

(1) Doppia cache ridondante con invalidazione incoerente. Lo stack è (esterno→interno) @transactional → @cached → @optimized_query. Sia @cached (models/caching/manager.py:444, get/set su cache_manager) sia @optimized_query (models/optimization/query_optimizer.py:29-66, get/set su lo STESSO cache_manager) implementano caching. Sono due livelli reali con chiavi diverse ("campionato:{id}" vs "update_campionato_classification:{hash(args)}"), stesso TTL (300) ma TAG DI…
- **[architettura] (PLAUSIBLE)** `models/playoff/services.py:348`
  - **Problema:** I path di not-found in PlayoffService sollevano ValueError generico ('Configurazione playoff non trovata', 'Campionato playoff non trovato', 'Qualificazione non trovata') invece delle eccezioni di dominio NotFoundError da models.exceptions. Le convenzioni del progetto mappano NotFoundError->404, ValueError generico->400.
  - **Scenario:** Una route che chiama es. _check_playoff_readiness / complete_playoff_campionato / admin_remove_player con un id inesistente riceve un ValueError che le route AJAX mappano a 400 invece del corretto 404, mascherando la natura 'risorsa non trovata' dell'errore e rendendo incoerente la gestione errori rispetto al resto del dominio.
  - **Verifica:** The underlying convention deviation is REAL: models/playoff/services.py raises generic ValueError for not-found conditions ("Configurazione playoff non trovata" L183/L253/L348/L680/L790, "Campionato playoff non trovato" L283/L378, "Qualificazione non trovata" L842) instead of the domain NotFoundError from models/exceptions.py. The project convention (models/CLAUDE.md "Domain Exceptions" + http_status_for_exception in models/exceptions.py) explicitly says these should be NotFoundError → HTTP 404, generic ValueError → 400. So as an architecture/consistency nit (the finding's own category=archite…
- **[architettura] (PLAUSIBLE)** `models/rating/handicap_service.py:327`
  - **Problema:** bulk_import_fargo_ratings ingoia ogni eccezione con un print() (non logger) e continua. Errori di import (utente inesistente, valore non valido) sono silenziati e non tracciati, e il falsy-check `if user_id and fargo_rating` salta righe con fargo_rating==0 o user_id==0.
  - **Scenario:** Import massivo di rating Fargo con alcune righe malformate: gli errori finiscono solo su stdout (perso in produzione PythonAnywhere), imported_count non riflette i fallimenti e l'admin crede che l'import sia riuscito mentre alcuni rating non sono stati scritti. Una riga con fargo_rating=0 (o user_id mancante codificato come 0) viene saltata senza segnalazione.
  - **Verifica:** Il nucleo del finding è reale e costruibile dal codice (models/rating/handicap_service.py:304-332). Il blocco `except Exception as e: print(...)` (righe 327-330) inghiotte ogni errore di import (utente inesistente, valore non valido, errori DB) inviandolo solo a stdout — in produzione su PythonAnywhere è di fatto perso, non c'è logger. Inoltre `imported_count` viene incrementato solo nel path di successo (riga 325), quindi non riflette né conteggia i fallimenti: l'admin riceve solo il numero di righe importate con successo e non sa quante e quali righe sono fallite. Questo è un difetto reale d…
- **[architettura] (CONFIRMED)** `models/events/notification_handlers.py:132`
  - **Problema:** Ogni handler avvolge il corpo in try/except che logga e inghiotte l'eccezione. Poiché l'eccezione non risale a EventBus.publish, il percorso di cattura Sentry/GlitchTip (_capture_handler_exception, base.py:262) non viene mai attivato per questi handler: il monitoraggio errori arricchito è di fatto bypassato.
  - **Scenario:** NotificationService.create_notification fallisce (es. errore DB transiente o utente anonimizzato). L'errore viene solo loggato come ERROR locale; nessun evento Sentry con event_type/handler_name/event_domain viene creato, riducendo l'osservabilità in produzione rispetto al design previsto in base.py.
  - **Verifica:** Il finding è accurato e costruibile dal codice. Flusso verificato: ogni handler in notification_handlers.py avvolge l'intero corpo in `try/except Exception` che logga e NON rilancia (es. linee 132-133, 168-169, 206-207, 242-243, 279-280, 309-310, 348-349, 396-400, 445-449, 470-471). Quindi `EventHandler.__call__` (base.py:106-114) chiama `self.handler_func(event)` che ritorna normalmente (nessuna eccezione propagata), e `EventBus.publish` (base.py:253-263) non entra mai nel ramo `except` di linea 256 → `_capture_handler_exception(e, event, handler)` a linea 262 non viene mai invocato per quest…
- **[architettura] (CONFIRMED)** `models/player/history_service.py:155`
  - **Problema:** L'intero blocco di query (join, filter su status/is_bye/is_trio, options con joinedload) e' duplicato pari pari due volte in get_match_history (righe 107-141 e 155-189) col commento 'pagination needs fresh query'. La prima query viene completamente materializzata con query.all() per le stats E poi rieseguita da capo per la paginazione: tutte le righe filtrate vengono caricate due volte. La duplicazione e' fragile (ogni modifica al filtro va replicata) e raddoppia il lavoro DB sul percorso caldo.
  - **Scenario:** Manutenzione: si aggiunge un nuovo criterio alla query base ma lo si applica solo a uno dei due blocchi duplicati -> stats e lista paginata divergono in modo difficile da individuare. Prestazioni: per ogni caricamento pagina, l'intero set filtrato viene letto due volte (una per .all() delle stats, una per paginate).
  - **Verifica:** Il finding e' accurato e verificabile nel codice di models/player/history_service.py::get_match_history. Il blocco di costruzione query (outerjoin Gara/Campionato/TrioMatch, filter su status==COMPLETED.value / is_bye / is_trio con le tre posizioni del trio, .options con joinedload) e' copiato verbatim due volte: righe 107-141 e righe 155-189. Anche order_by e _apply_match_filters sono ripetuti (144-147 vs 190-191). La riga 150 materializza tutto il set filtrato con query.all() per le stats (_calculate_match_stats), poi la seconda query rilegge da capo lo stesso set per query.paginate() (riga 1…
- **[performance] (PLAUSIBLE)** `models/notification/services.py:411`
  - **Problema:** auto_delete_by_user_preferences esegue una query DELETE-select per ogni preference dentro il loop (N+1). In modalita cron (user_id=None) scandisce TUTTE le preference di tutti gli utenti con una query notifiche per ognuna, e materializza .all() per cancellare riga per riga invece di una bulk delete.
  - **Scenario:** Job schedulato chiama auto_delete_by_user_preferences() senza user_id su una base con migliaia di utenti x molti tipi di notifica: si generano migliaia di SELECT (riga 411) piu DELETE singole (riga 427), con caricamento in memoria di tutte le notifiche scadute. Su SQLite questo allunga la transazione e aumenta il rischio di lock/lentezza del task notturno; una bulk delete con filtro composito sarebbe ordini di grandezza piu efficiente.
  - **Verifica:** Il finding descrive correttamente il codice in models/notification/services.py: il loop su `preferences` (riga 404) esegue per ogni preference una query `Notification.query.filter(...).all()` (riga 411) e poi cancella riga per riga con `db.session.delete()` (righe 426-427) invece di una bulk delete. In modalita cron (user_id=None) il filtro per utente non viene applicato (righe 400-402), quindi si scandiscono le preference di tutti gli utenti generando N+1 SELECT + DELETE singole e materializzando in memoria tutte le notifiche scadute. Quindi il pattern di inefficienza esiste davvero nel codic…
- **[performance] (CONFIRMED)** `models/dashboard/dashboard_service.py:397`
  - **Problema:** for_player esegue standalone_q().all() due volte: standalones_all alla riga 332 e all_standalone_garas alla riga 397, sugli stessi identici dati. Doppia query (con ORDER BY) ad ogni render della dashboard player. standalones_all serve solo a build_selector_items e puo' essere riusato.
  - **Scenario:** Ogni caricamento della dashboard giocatore interroga due volte la tabella gare per le standalone (stesso filtro/ordinamento). Riusando standalones_all per build_unified_items e _standalone_completed_tail si dimezza il costo di quel path senza cambiare comportamento.
  - **Verifica:** In models/dashboard/dashboard_service.py, for_player() esegue standalone_q().all() due volte: riga 332 (standalones_all, usato solo per build_selector_items a riga 423) e riga 397 (all_standalone_garas, usato per build_unified_items a riga 398-400 e _standalone_completed_tail a riga 407). standalone_q() (definita in models/dashboard/query_builders.py:54-60) e' una query deterministica con filtro fisso (Gara.campionato_id.is_(None)) e ORDER BY fisso, eseguita sulla stessa session nello stesso request: le due .all() restituiscono dati identici. La seconda query e' quindi ridondante. Riusando sta…
- **[performance] (CONFIRMED)** `models/challenge/models.py:157`
  - **Problema:** get_statistics esegue piu' volte la stessa query lazy='dynamic': attempts_query.count(), .with_entities(...).distinct().count(), .all() (riga 146) e poi itera di nuovo 'attempts_query' (riga 157) che ri-esegue la SELECT. Sui rami pass/fail si materializzano i tentativi due volte.
  - **Scenario:** Per una challenge con molti tentativi completati, get_admin_statistics itera su tutte le challenge e ognuna esegue 3-4 query separate piu' una ri-esecuzione della query nel generatore pass_rate. Su catalogo grande la vista statistiche admin fa N*4 query (effetto N+1 amplificato), degradando la pagina; basterebbe materializzare scores/passed una sola volta.
  - **Verifica:** Real (low-severity) redundancy, demonstrable from the code. `Challenge.attempts` is `lazy="dynamic"` (models/challenge/models.py:88-93), so `attempts_query` is a query object, not a cached collection. In `get_statistics` it is executed multiple times against the same rows: count() at line 129, a distinct count at line 131, `.all()` materializing every row at line 146, and then — on the pass/fail branch — iterated again at line 157 (`sum(1 for attempt in attempts_query if attempt.passed)`), which re-runs the full SELECT even though the rows were already loaded into `scores` at line 146. The pas…
- **[performance] (CONFIRMED)** `models/exam/services.py:204`
  - **Problema:** get_director_statistics() esegue query N+1: itera sugli exam chiamando exam.attempts.filter_by(...).count() per ognuno (line 205), poi di nuovo itera gli attempt completati (line 211), e infine chiama exam.get_statistics() per ogni exam (line 223) che a sua volta fa piu' query e carica tutti gli attempt in memoria.
  - **Scenario:** Un director con molti exam e molti attempt apre la dashboard statistiche: vengono emesse decine/centinaia di query (almeno 3-4 round-trip DB per exam piu' il caricamento completo degli attempt), degradando il tempo di risposta in modo lineare col numero di exam/attempt.
  - **Verifica:** Real N+1 query pattern, correctly identified. The `Exam.attempts` relationship is defined with `lazy="dynamic"` (models/exam/models.py:46-51), so every access issues a fresh SQL query instead of using preloaded data. In `get_director_statistics()` (models/exam/services.py:199-224), for EACH exam in the director's list it runs: (1) line 205 `exam.attempts.filter_by(completed=True).count()` → 1 query; (2) line 211 iterating `exam.attempts.filter_by(completed=True)` → 1 query; (3) line 223 `exam.get_statistics()` which internally runs another count() (models/exam/models.py:92), a distinct().count…
- **[performance] (PLAUSIBLE)** `routes/player/exports.py:217`
  - **Problema:** _collect_user_data esegue db.session.refresh(privacy) avvolto in try/except Exception generico che inghiotte qualsiasi errore e ricade su default 'tutto nascosto'. Un errore transitorio (es. lock SQLite nel thread background) fa sì che l'export GDPR riporti privacy_settings tutte False invece dei valori reali dell'utente, senza segnalarlo.
  - **Scenario:** Durante la generazione GDPR in background, refresh(privacy) fallisce per contesa SQLite; l'export consegnato all'utente contiene privacy_settings completamente errate (tutte False), violando l'accuratezza richiesta da un export GDPR.
  - **Verifica:** The finding describes a real, constructible behavior. In routes/player/exports.py, `_collect_user_data` runs inside a background `threading.Thread` under `with app.app_context()` (lines 404, 506). At line 215 `PrivacyService.get_privacy_settings(user_id)` returns the user's real, attached settings object; line 217 then does a redundant `db.session.refresh(privacy)` wrapped in a broad `try/except Exception` (lines 214-235). On ANY exception the except branch (line 226) only logs a `logger.warning` and falls back to privacy_data with every flag set to False (lines 228-235), which matches the mod…

---

## Da ri-verificare a mano (verificatore interrotto da rate-limit)

> **CHIUSO 2026-06-10**: triage completato (3 agenti + verifica manuale).
> Esiti: 13 confermati e fixati (batch 7 PR #33, batch 8 PR #34), 4 falsi
> positivi, 1 gia' risolto. Con i batch 1-8 (PR #22-#34) TUTTI i finding di
> questo report risultano processati. Dettagli nel post-mortem in memoria
> di sessione e nei body delle PR.

Questi finding provengono dai finder ma NON sono stati verificati. Trattali come *candidati*, non come bug confermati.

### Severità: HIGH (5)

- **[correttezza]** `routes/admin/match/challenges.py:17`
  - **Problema:** Le route admin POST /admin/match/record_challenge_attempt e /record_challenge_attempts usano @match_manager_required, decorator che fa `match_id = kwargs.get('match_id'); if match_id is None: abort(400)` (utils/permissions.py:81-83). Ma queste route NON hanno alcun parametro `<int:match_id>` nell'URL, quindi match_id e' sempre None e il decorator aborta SEMPRE con HTTP 400 prima di eseguire la view.
  - **Scenario:** Un admin/direttore apre il match detail di una gara Random e inserisce i tentativi challenge: il template templates/components/_match_challenge_input.html fa fetch a '/admin/match/record_challenge_attempt(s)' (righe 297 e 345). Ogni chiamata ritorna 400 'Bad request' senza mai registrare nulla. La funzionalita' di registrazione challenge da pannello admin e' completamente morta (regressione silenziosa: nessun errore lato server, solo 400).
- **[correttezza]** `routes/gamification/dashboard.py:134`
  - **Problema:** Sulla pagina pubblica /leaderboards il template fa entry.user.username, ma entry.user e' una relationship verso User soggetta al filtro globale soft-delete (with_loader_criteria su User.deleted_at IS NULL). Le LeaderboardEntry sono uno snapshot cache che memorizza user_id al momento del refresh; se un utente in classifica viene poi anonimizzato/soft-deleted, entry.user si risolve a None e il render del template solleva UndefinedError/AttributeError (None deref).
  - **Scenario:** Un utente che e' nella top-20 XP/level/streak chiede la cancellazione account -> user.anonymize() (deleted_at valorizzato). Finche' la cache leaderboard non scade (TTL fino a 1h), get_leaderboard restituisce ancora la sua entry con user=None. Qualsiasi visitatore anonimo che apre /gamification/leaderboards ottiene un 500: entry.user.username su None. Pagina pubblica down per tutti finche' la cache non si rinfresca.
- **[correttezza]** `routes/individual_match/matches.py:192`
  - **Problema:** confirm_result confronta match.status.value con "completed", ma un IndividualMatch dopo la conferma bilaterale passa a VALIDATED (vedi match_models.py:293 e base_match.py:217), mai a COMPLETED. Quindi quando entrambi i giocatori confermano e il match e' di fatto concluso, il check 'completed' resta False.
  - **Scenario:** Due giocatori confermano il risultato. Alla seconda conferma il match diventa VALIDATED. La route emette l'evento SSE 'result_confirmed' invece di 'match_completed', risponde JSON con completed=False e mostra il flash 'Risultato confermato! In attesa dell'altro giocatore' anche se non c'e' piu' nessuno in attesa. Frontend e SSE non vedono mai il completamento. Va usato MatchStatus.is_finished(match.status) (che copre completed|validated) invece del confronto letterale con 'completed' alle righe 192, 197, 205, 216.
- **[correttezza]** `utils/feature_flags.py:244`
  - **Problema:** _user_role() classifica come "player" un utente con role=='player' aggiunto come co-direttore di una gara (is_director controlla solo role=='director'). In produzione tutti gli endpoint admin.competition.* di gestione sono listati come {"director"}, quindi il middleware ADR-028 (app.py:216) restituisce 404 prima ancora di raggiungere la route, anche se can_manage_competition() concederebbe l'accesso. Il codebase supporta esplicitamente i co-direttori player (models/user/permissions.py:89).
  - **Scenario:** Un admin aggiunge un giocatore (role=player) come co-direttore di una gara via add_director. In produzione quel giocatore apre la pagina di gestione del turno (es. admin.competition.start_first_round): _user_role() ritorna 'player', non presente nel set {'director'}, il before_request enforce_endpoint_allowlist fa abort(404). L'utente che dovrebbe poter gestire la gara vede un 404 inspiegabile, pur passando i controlli di autorizzazione reali.
- **[sicurezza]** `utils/encryption.py:82`
  - **Problema:** EncryptionManager.decrypt() inghiotte qualsiasi eccezione e ritorna stringa vuota invece di propagare l'errore. Usato da EncryptedString.process_result_value (models/fields.py:26) per campi PII come email e phone. Qualunque fallimento di decrittazione (chiave/salt diversi tra ambienti, rotazione ENCRYPTION_KEY, dato corrotto) trasforma silenziosamente il campo in "" alla lettura.
  - **Scenario:** In produzione ENCRYPTION_KEY viene cambiata/ruotata (o un DB di dev con la default key viene aperto con una key diversa). Al primo SELECT degli utenti, ogni email decrittata fallisce e ritorna "": il login per email non trova piu' nessuno, il reset password invia a indirizzo vuoto, le notifiche email saltano, e l'errore non compare da nessuna parte se non in un print su stdout. Corruzione silenziosa di dati critici di autenticazione.

### Severità: MEDIUM (5)

- **[correttezza]** `routes/admin/match/scoring.py:41`
  - **Problema:** add_rack_result passa hardcoded reported_by_id=1 ('Admin user ID') invece di current_user.id. L'attribuzione di chi ha inserito il rack viene sempre registrata sull'utente con id=1.
  - **Scenario:** Un direttore (non admin) inserisce i rack di un match dal pannello admin: il rack viene attribuito all'utente id=1 (admin seed) e non al direttore reale. L'audit trail di 'reported_by' e' falsato per tutti gli inserimenti admin/director. Se l'utente id=1 fosse stato anonimizzato/soft-deleted o non esistesse (DB ripopolato con id diversi), il FK reported_by_id=1 punterebbe a un record inesistente o cancellato.
- **[sicurezza]** `routes/admin/competition/inscriptions.py:234`
  - **Problema:** add_director e' protetto solo da @gara_manager_required: qualsiasi direttore che gestisce questa gara puo' aggiungere come co-direttore un user_id arbitrario preso dal form, senza verificare che quell'utente sia gia' director o eligibile. GaraService.add_director potrebbe quindi concedere privilegi di gestione gara a un account player qualsiasi scelto dall'attaccante.
  - **Scenario:** Un direttore della gara invia POST /admin/gara/<id>/add_director con user_id=<id di un complice player>. Se il service crea una DirectorAssignment senza verificare il ruolo del target, il complice ottiene permessi di gestione (inserire risultati, validare match, modificare iscrizioni) sulla gara, scalando da player a gestore senza approvazione admin.
- **[correttezza]** `routes/gamification/admin.py:122`
  - **Problema:** admin_create_quest parsa input con conversioni non protette: int(request.form.get('requirement_target', 10)), int(xp_reward), datetime.fromisoformat(start_date_str), datetime.fromisoformat(end_date_str) e QuestType[quest_type.upper()]. Nessuno e' in try/except: int()/fromisoformat sollevano ValueError, l'accesso enum per chiave solleva KeyError. La ValueError verrebbe persa col 500 generico, ma il KeyError non e' nemmeno coperto da handle_service_action (avviene PRIMA, nel parsing della route).
  - **Scenario:** Admin (o director con accesso) invia il form con quest_type='foobar' o start_date='2026-13-99' o requirement_target='ten' -> QuestType['FOOBAR'] KeyError / datetime.fromisoformat ValueError sollevati nel corpo della route prima di handle_service_action -> 500 non gestito invece di un flash di errore validazione. Stesso pattern in admin_grant_freeze (StreakType[streak_type_str] KeyError, riga ~407) e admin_grant_xp/admin_create_achievement (int() non protetti).
- **[correttezza]** `routes/individual_match/proposals.py:179`
  - **Problema:** create_proposal POST accede a data["scheduled_at"] e (riga 197) data["location"] con indicizzazione diretta; se il campo manca nel JSON/form sollevano KeyError, NON catturato dal solo 'except ValueError' -> 500 non gestito. Stesso pattern in matches.py add_rack/complete_match con int(data["winner_id"]) e remove_rack con data["player_id"].
  - **Scenario:** Un client API (o richiesta malformata) invia POST /match/proposals/create con JSON privo di 'scheduled_at'. data["scheduled_at"] lancia KeyError che bypassa il blocco except ValueError, restituendo HTTP 500 con stack trace invece di un 400 pulito. Stesso effetto inviando POST /match/matches/<id>/racks/add senza 'winner_id'. Usare data.get(...) con validazione esplicita o catturare anche KeyError/TypeError.
- **[correttezza]** `routes/individual_match/matches.py:448`
  - **Problema:** rematch perde il formato del match originale. Costruisce i parametri con 'distance': match.distance or 5 e non passa mai 'match_format'. create_proposal (proposals.py:207-227) decide single/multi/free SOLO da data.get('match_format','single'), quindi ogni rematch diventa single-set race-to-5: per i match free format (distance=None -> 5) e per i multi-set (is_multi_set passato ma ignorato) la configurazione viene silenziosamente persa.
  - **Scenario:** Un giocatore completa un match free format (distance=None) o multi-set e clicca 'Gioca ancora'. Il rematch reindirizza a create_proposal con match_format implicito='single' e distance=5: la nuova proposta nasce come singolo set race-to-5, ignorando il formato originale. L'utente deve riconfigurare manualmente; se non se ne accorge crea un match con regole diverse da quelle attese.

### Severità: LOW (8)

- **[correttezza]** `routes/admin/venue.py:358`
  - **Problema:** toggle_venue_status legge value = data.get('value') dal JSON e lo passa direttamente a set_venue_active/set_venue_verified senza coercizione a bool. Se il client invia value mancante (None) o una stringa ('false'), il valore non-bool finisce nel campo DB.
  - **Scenario:** Un client (o richiesta malformata) invia {'field':'is_active'} senza 'value', oppure {'value':'false'} come stringa: con None il flag is_active viene impostato a None (NULL) anziche' False; con la stringa 'false' (truthy) la sala viene erroneamente ATTIVATA invece che disattivata. Stato venue incoerente che puo' nascondere/mostrare sale nel listing pubblico.
- **[correttezza]** `routes/admin/competition/rounds.py:45`
  - **Problema:** start_first_round fa gara = db.session.get(Gara, gara_id) senza controllo None, poi usa `gara and ...` solo nel ramo del messaggio. Se gara fosse None (gia' cancellata tra il check del decorator e questa get), RoundService.start_first_round(gara_id) riceverebbe un id orfano; il messaggio di successo verrebbe comunque mostrato nel ramo else anche se l'avvio e' fallito a monte.
  - **Scenario:** Race: il decorator gara_manager_required valida i permessi, poi un altro processo soft-deleta la gara; db.session.get ritorna None. RoundService solleva un errore catturato dall'except Exception generico che mostra 'Errore interno del server' (accettabile), ma il pattern `gara and gara.matchmaking_strategy` maschera la None invece di abortire esplicitamente con 404, rendendo il comportamento dipendente da quale eccezione viene sollevata internamente.
- **[architettura]** `routes/gamification/admin.py:345`
  - **Problema:** Diversi success_message sono f-string non tradotte invece di _(): 'Concessi {xp_amount} XP all'utente.' (345), 'Concessi {freeze_count} freeze all'utente.' (416). Idem in config.py (es. "XP rate '{key}' aggiornato" 94, "Parametro '{key}' aggiornato" 143, "Level unlock ... aggiunto" 169) e features.py. Viola la regola i18n del progetto (TUTTE le stringhe utente in _()), e l'apostrofo non escapato in 'all'utente' e' fragile.
  - **Scenario:** Con locale EN attivo questi messaggi restano in italiano hard-coded e non passano dalla pipeline pybabel (non estratti). L'admin in inglese vede stringhe miste IT/EN; nessun crash ma incoerenza i18n e debito che si accumula a ogni nuova action copiata da questo pattern.
- **[architettura]** `routes/individual_match/matches.py:73`
  - **Problema:** Quasi tutti i messaggi flash e JSON di queste route sono hardcoded in inglese ('Match started successfully!', 'Error starting match', 'Access denied to this match', 'Error loading matches', ecc.) e NON wrappati in _(); solo forfeit_match e decline_proposal usano la traduzione. Viola la regola i18n-everything del progetto (app Italian-first).
  - **Scenario:** Un utente con locale italiano avvia/annulla un match o incontra un errore: riceve messaggi in inglese non tradotti ('Match cancelled successfully!', 'Access denied.'), incoerenti col resto dell'interfaccia. Le stringhe non sono nemmeno estraibili da pybabel perche' prive di _().
- **[sicurezza]** `routes/individual_match/proposals.py:29`
  - **Problema:** Gli handler che fanno 'except Exception as e' (proposal_list, proposal_detail, match_list/detail in matches.py, dashboard/user_statistics/admin_overview in views.py) flashano f-string con str(e) direttamente all'utente, esponendo messaggi di errore interni/eccezioni non previste invece di un messaggio generico.
  - **Scenario:** Un errore interno inatteso (es. eccezione di SQLAlchemy o AttributeError) durante il caricamento di una proposta viene catturato dal generico except Exception e str(e) viene mostrato nel flash all'utente finale, potendo rivelare dettagli implementativi (nomi tabelle/colonne, struttura interna). Andrebbe loggato lato server e mostrato un messaggio generico e tradotto.
- **[sicurezza]** `routes/rating.py:282`
  - **Problema:** get_user_category_api (solo @login_required) restituisce la categoria di QUALSIASI user_id passato in URL, senza verificare che sia l'utente corrente o un permesso director. IDOR su dati di profilo altrui.
  - **Scenario:** Un player autenticato chiama GET /api/user/<altro_user_id>/category iterando gli id: ottiene categoria, data assegnazione e stato attivo di tutti gli altri giocatori, enumerando informazioni di profilo non sue.
- **[correttezza]** `utils/image_paths.py:126`
  - **Problema:** save_challenge_image chiama is_allowed_file(image_file.filename) fuori dal blocco try. Se image_file.filename e' None (FileStorage senza filename in certe richieste multipart malformate), is_allowed_file esegue '.' in None -> TypeError non gestito che propaga come 500. La guardia 'not image_file' non copre il caso filename=None su file part presente.
  - **Scenario:** Un client invia una richiesta multipart con un campo file 'image' presente ma senza nome file (filename=None). request.files.get('image') ritorna un FileStorage truthy; is_allowed_file(None) esegue '.' in None sollevando TypeError prima del try/except, restituendo un 500 invece di un fallimento controllato (return None) sulla creazione/modifica della challenge.
- **[correttezza]** `utils/status_ui.py:93`
  - **Problema:** Le stringhe di fallback 'Sconosciuto' nei vari mapping (gara riga 93, campionato 124, match 143, director_request 159, playoff 177) sono letterali non wrappate in _(), a differenza di tutte le altre etichette di stato. Viola la convenzione i18n-everything del progetto e queste stringhe non vengono mai tradotte.
  - **Scenario:** Un oggetto con uno status non mappato (es. nuovo stato enum aggiunto ma non ancora nel mapping, o dato legacy) viene reso in UI: il badge mostra 'Sconosciuto' in italiano anche con locale EN attivo, perche' la stringa non passa per gettext. Inconsistenza di traduzione visibile all'utente in lingua inglese.
