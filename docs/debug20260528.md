# sessione di debug manuale

in questo file prendo appunti sui bug che riscontro. 

In questa sessione voglio provare a creare un campionato con 2 gare e playoff elite e academy. 

## bug riscontrati

1) ~~ho creato gara 1 e gara 2. le iscrizioni di gara 2 si aprono tra qualche giorno. mi sono loggato come altro director e nella dashboard vedo tra le gare attive anche gara 2, con badge iscrizioni aperte (non e' vero, peche' ho solo impostato le date di apertura e chiusura, ma ancora non sono aperte). si vede il pulsante iscriviti, ma se lo clicco mi viene un messaggio flash "le iscrizioni non sono disponibili". Stessa cosa per dashboard player. Inoltre nella card non e' scritto data e ora di inizio e fine iscrizioni.~~ FIXED: nuovo stato derivato `INSCRIPTION_NOT_YET_OPEN`, badge "Iscrizioni non ancora aperte" + data apertura/chiusura sulle card.

2) ~~non so se e' un vero bug, ma quando ho cliccato fill gara nel debug footer, mi ha aggiunto 7 giocatori (player1, ..., player7). la gara aveva gia' 3 iscritti e il limite minimo impostato per la gara era 6. mi aspettavo che fossero aggiunti solo 3 giocatori e mi aspettavo che fossero quelli del quick login. update: in gara 2 ho fatto subito fill gara e ha aggiunto i 7 player, quindi e' il comportamento predefinito. invece dovrebbe essere: aggiungi il numero di giocatori necessario per raggiungere il minimo, scegliendoli tra quelli del quick login. Andrebbe aggiunto un altro pulsante nel debug footer "iscrivi giocatore" che iscrive il prossimo non ancora iscritto (a meno che non si sia raggiunto il massimo per la gara oppure non ci siano piu' giocatori disponibili)~~ FIXED: fill_gara ora si ferma al MIN scegliendo dai quick-login player; nuovo bottone "+ Iscrivi" iscrive uno alla volta fermandosi al MAX.

3) ~~ho avviato la partita e nella vista admin/gara/22 nella sezione Partite, i turni sono elencati dal turno 3 al turno 1. Sto vedendo da desktop. il turno 1 dovrebbe essere il primo in alto. In generale l'interfaccia dovrebbe mostrare in alto le parti azionabili, senza che l'utente debba scrollare. Inoltre, vicio a Turno 1 dovrebbe esserci la specifica del turno. Ad esempio in questo caso dovrebbe comparire (9-ball, 5 rack esatti) mentre vicino a turno 2 (8-ball, 4 rack esatti)~~ FIXED: turni in ordine crescente (current_round in cima) + sottotitolo con discipline e distance per turno.

5) ~~ho cliccato Complete Round nel debug footer, ma i tavoli non vengono assegnati agli altri match in attesa. nell'assegnazione automatica del tavolo andrebbero privilegiati i match del turno immediatamente successivo.~~ FIXED: debug_complete_current_round ora rilascia i tavoli dei match completati e chiama assign_available_tables (sorting per round_number+id privilegia automaticamente il turno successivo).

6) ~~la gara e' random. dopo aver premuto complete round, tutti i match del primo turno hanno i punteggi, ma se poi assegno i tavoli al secondo turno e premo di nuovo complete round, no succede nulla. Va aggiunto anche un pulsante in debug footer "complete next match" che completa a caso il prossimo match ancora non completato e "complete gara" che continua a completare match fino a quando sono tutti completati. In questo caso bisogna capire se e' terminato il turno (se strategia e' amalfi o comunque diversa da random) e se bisogna far partire il turno successivo~~ FIXED: nuove debug action `Complete Match` (un solo match random) e `Complete Gara` (loop con avanzamento turni via start_next_round, idempotente per strategie pre-generate).

7) ~~complete round con un trio non ha funzionato. ha riempito tutti i match che non sono trio.~~ FIXED: _debug_random_score_trio distribuisce random le vittorie rispettando max_per_player e delega a TrioScoringService.set_result_direct.

8) ~~ho modificato i playoff mettendo posizioni 1-8 ... nessun giocatore qualificato~~ FIXED: catena di 3 fix (a) terminate_campionato promuove TOURNAMENT_COMPLETED → COMPLETED invece di soft-eliminare; (b) start_playoff chiama update_campionato_classification per popolare Classification DB; (c) _meets_minimum_requirements usa Classification.gare_played invece di filter su Gara.status=='completed'.

9) ~~ho cancellato i playoff e non ci sono piu' pulsanti per aggiungere nuove configurazioni di playoff. prima c'era il pulsante per aggiungerne altre.~~ FIXED: blocco Playoff sempre visibile post-terminate (rimosso AND has_playoff_configurations dalla guard); ramo else "Nessuna configurazione" ora include il form "Aggiungi configurazione".

10) ~~in /admin/campionato/wizard/step2 ho selezionato Trio come gestione dispari e mi e' venuto un messaggio che dice Solo per distanze 2-5, ma dovrebbe essere 2-7~~ FIXED: aggiornata la stringa nel template wizard step2 + commento in constants.py allineati a TrioConfig.MAX_DISTANCE=7.

11) ~~in gara 2 del campionato ho aperto le iscrizioni, con data di inizio non ora, ma tra un giorno. In admin/gara/25 nella sezione Gestione e' comparso un badge "sconosciuto" e  non c'e' piu' il pulsante per le iscrizioni, invece  dovrebbe comparire per poter modificare in ogni momento. penso che sia legato al bug che hai risolto prima. Va bene non permettere iscrizioni se la data di inizio non  e' passata, ma il director/admin deve poter modificare.~~ FIXED: i template _gara_management.html e _index_campionato_cards.html usavano un valore obsoleto `inscription_not_started` per il branch "pre-apertura", ora allineati a `inscription_not_yet_open`. Il branch già conteneva il bottone "Modifica Date".

12) ~~ho premuto due volte complete round, ma alla seconda volta mi ha detto che non c'e' nessun round da completare ... complete match dovrebbe prenderne uno e completarlo. Il primo che capita, non e' importante quale.~~ FIXED: nuovo helper `_debug_first_active_round` ritorna il primo round con match incomplete (non `gara.current_round` che resta fermo per strategie pre-generate). debug_complete_current_round usa il primo round attivo; debug_complete_next_match pesca da TUTTI i round con match incomplete; debug_complete_gara fa lo stesso loop robusto.

13) ~~ho premuto complete match e ha completato un match che non aveva un tavolo assegnato. Questo e' un errore. deve completare un match qualsiasi, ma di quelli che hanno tavolo assegnato e sono in corso~~ FIXED: nuovo helper `_debug_completable_matches` filtra solo match PLAYING con `table_assignment` valorizzato (escluso bye); `debug_complete_next_match` pesca da quelli. 2 test di regressione in `test_debug_complete_next_match_and_gara.py` (nessun completamento se nessun match ha tavolo + helper restituisce solo PLAYING+tavolo).

14) ~~quando tutte le gare del campionato sono state giocate e terminate, il pulsante non deve essere termina campionato se c'e' un playoff, ma "passa alla fase playoff" o qualcosa di simile~~ FIXED: nuovi metodi `Campionato.all_gare_concluded()` (considera status COMPLETED o derivato TOURNAMENT_COMPLETED, non ROUND_COMPLETED) e `is_ready_for_playoff_transition()`; in `campionato_detail.html` il bottone diventa "Passa alla fase playoff" (verde) con messaggio di conferma positivo quando tutte le gare sono concluse + c'e' un playoff configurato. Azione invariata (terminate_campionato). 7 test unit in `test_terminate_campionato.py`.

15) ~~la notifica di invito ai playoff di conferma o rifiuta la partecipazione, ma non c'e' nessun modo per confermare o rifiutare.~~ FIXED: le route player (`/player/playoff/invitation|confirm|decline/<id>`) e la pagina con i bottoni esistevano gia', ma (a) la notifica era bulk senza `action_url`/`qualification_id` quindi nessun link cliccabile, (b) le route non erano in `ENDPOINT_ROLES` (admin-only in prod + bottone nascosto da `url_visible`). Ora `_send_playoff_invitations` crea una notifica per-qualifica con `action_url=/player/playoff/invitation/<id>` + `action_text`, e le 3 route sono in allowlist (player/director). Test: deep-link notifica (unit) + GET pagina invito con bottoni (integration).

16) ~~nella pagina delle notifiche l'auto cancellazione a 30 gg dovrebbe essere impostata per default. ogni volta che si apre dovrebbero essere cancellate tutte quelle scadute rispetto ai parametri di auto cancellazione~~ FIXED: nuovi metodi `NotificationService.get_global_auto_delete_days` (default `DEFAULT_AUTO_DELETE_DAYS=30` se mai configurato, None=disattivato), `set_global_auto_delete_days` (enable/disable esplicito, ripara il bug per cui disattivare non persisteva None su preferenza esistente) e `auto_delete_for_user` (cancella TUTTE le notifiche scadute di QUALSIASI tipo, non solo SYSTEM_ANNOUNCEMENT). La route `/player/notifications` ora applica la pulizia ad ogni apertura col default 30 e pre-popola il form col valore corretto. Test: 6 unit (default/set/disable/all-types/disabled/keeps-unread) + 2 integration (apertura pagina cancella scadute / rispetta disattivazione).