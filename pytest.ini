[pytest]
minversion = 7.4
addopts = -q -ra --strict-markers -m "not legacy"
testpaths = tests/new
markers =
    unit: test di unità
    integration: test di integrazione con DB/app
    e2e: test end-to-end su route
    legacy: vecchi test (esclusi)
filterwarnings =
    ignore::DeprecationWarning
